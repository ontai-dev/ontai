# Guardian Service
# Generated by: compiler enable (phase 2 guardian-deploy)
# Multi-port Service: webhook (443→9443), gRPC (9090→9090), metrics (8080→8080).
# caBundle in ValidatingWebhookConfigurations references this Service.
# guardian-schema.md §3.
---
apiVersion: v1
kind: Service
metadata:
  annotations:
    ontai.dev/managed-by: compiler
  labels:
    app.kubernetes.io/component: webhook
    app.kubernetes.io/name: guardian
    ontai.dev/managed-by: compiler
  name: guardian
  namespace: seam-system
spec:
  ports:
  - name: webhook
    port: 443
    protocol: TCP
    targetPort: 9443
  - name: grpc
    port: 9090
    protocol: TCP
    targetPort: 9090
  - name: metrics
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app.kubernetes.io/name: guardian
