# Bootstrap RBACPolicy — seam-platform-rbac-policy
# Generated by: compiler enable (phase 1 guardian-bootstrap)
# Governs all operator RBACProfiles during the management cluster bootstrap window.
# enforcementMode=audit: Guardian records violations but does not block during bootstrap.
# Tighten maximumPermissionSetRef and enforcementMode post-bootstrap.
# guardian-schema.md §6.
---
apiVersion: security.ontai.dev/v1alpha1
kind: RBACPolicy
metadata:
  labels:
    ontai.dev/managed-by: compiler
  name: seam-platform-rbac-policy
  namespace: seam-system
spec:
  allowedClusters:
  - management
  enforcementMode: audit
  maximumPermissionSetRef: seam-bootstrap-ceiling
  subjectScope: platform
