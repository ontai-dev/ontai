# Platform, Wrapper, seam-core CRD Definitions
# Generated by: compiler enable (phase 3 platform-wrapper)
# Groups: platform.ontai.dev, infra.ontai.dev, infrastructure.ontai.dev
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: clustermaintenances.platform.ontai.dev
spec:
  group: platform.ontai.dev
  names:
    kind: ClusterMaintenance
    listKind: ClusterMaintenanceList
    plural: clustermaintenances
    shortNames:
    - cmaint
    singular: clustermaintenance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .status.conditions[?(@.type=="Paused")].status
      name: Paused
      type: string
    - jsonPath: .status.conditions[?(@.type=="WindowActive")].status
      name: WindowActive
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ClusterMaintenance is a maintenance window gate for a Talos cluster.

          Dual-path CRD governed by spec.capi.enabled on the owning TalosCluster:
            - For CAPI-managed clusters (capi.enabled=true): sets
              cluster.x-k8s.io/paused=true on the CAPI Cluster when no active window
              exists and blockOutsideWindows=true. Pause halts all CAPI reconciliation
              until the window opens and the annotation is lifted.
            - For management cluster (capi.enabled=false): blocks Conductor Job
              admission for the cluster during restricted periods.

          platform-schema.md §5.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterMaintenanceSpec defines the desired state of ClusterMaintenance.
            properties:
              blockOutsideWindows:
                description: |-
                  BlockOutsideWindows controls whether operations are blocked when no active
                  window exists. When false (default), operations are permitted at any time.
                  When true and no active window exists:
                    - CAPI path: sets cluster.x-k8s.io/paused=true on the CAPI Cluster, halting
                      all CAPI reconciliation until the window opens.
                    - Non-CAPI path: blocks Conductor Job admission for this cluster.
                type: boolean
              clusterRef:
                description: ClusterRef references the TalosCluster this maintenance
                  gate controls.
                properties:
                  name:
                    description: Name is the object name.
                    type: string
                  namespace:
                    description: Namespace is the object namespace. May be empty for
                      cluster-scoped objects.
                    type: string
                required:
                - name
                type: object
              lineage:
                description: |-
                  Lineage is the sealed causal chain record for this root declaration.
                  Authored once at object creation time and immutable thereafter.
                  seam-core-schema.md §5, CLAUDE.md §14 Decision 1.
                properties:
                  creatingOperator:
                    description: |-
                      CreatingOperator identifies the Seam Operator that created this object.
                      This is a structured identity carrying the operator name and its deployed
                      version at creation time.
                    properties:
                      name:
                        description: |-
                          Name is the canonical name of the Seam Operator (e.g., platform, guardian,
                          wrapper, conductor).
                        type: string
                      version:
                        description: |-
                          Version is the deployed version of the operator at the time the object was
                          created (e.g., v1.26.5-r3). This allows audit tooling to correlate objects
                          with the operator version that produced them.
                        type: string
                    required:
                    - name
                    - version
                    type: object
                  creationRationale:
                    description: |-
                      CreationRationale is the reason this object was created, drawn from the
                      Seam Core controlled vocabulary defined in rationale.go. It is not a
                      free-text field.
                    enum:
                    - ClusterProvision
                    - ClusterDecommission
                    - SecurityEnforcement
                    - PackExecution
                    - VirtualizationFulfillment
                    - ConductorAssignment
                    - VortexBinding
                    type: string
                  rootGenerationAtCreation:
                    description: |-
                      RootGenerationAtCreation is the metadata.generation of the root declaration
                      at the time this object was created. Together with RootUID, it provides a
                      complete temporal anchor for the derivation record.
                    format: int64
                    type: integer
                  rootKind:
                    description: |-
                      RootKind is the kind of the root declaration that caused this object to
                      exist (e.g., TalosCluster, PackExecution, RBACPolicy).
                    type: string
                  rootName:
                    description: RootName is the name of the root declaration.
                    type: string
                  rootNamespace:
                    description: RootNamespace is the namespace of the root declaration.
                    type: string
                  rootUID:
                    description: |-
                      RootUID is the UID of the root declaration at the time this object was
                      created. Used to verify that no root declaration replacement has occurred.
                    type: string
                required:
                - creatingOperator
                - creationRationale
                - rootGenerationAtCreation
                - rootKind
                - rootName
                - rootNamespace
                - rootUID
                type: object
              windows:
                description: |-
                  Windows is the list of maintenance windows during which operations are permitted.
                  When empty, operations are always permitted (unless blockOutsideWindows=true
                  with no windows, in which case operations are always blocked).
                items:
                  description: |-
                    MaintenanceWindow defines a time window during which maintenance operations
                    are permitted.
                  properties:
                    durationMinutes:
                      description: DurationMinutes is the length of the maintenance
                        window in minutes.
                      format: int32
                      type: integer
                    name:
                      description: Name is an optional label for this window.
                      type: string
                    start:
                      description: |-
                        Start is the window start time in cron format (e.g., "0 2 * * 6" for
                        02:00 every Saturday UTC).
                      type: string
                    timezone:
                      description: |-
                        Timezone is the IANA timezone for interpreting the cron schedule.
                        Defaults to UTC.
                      type: string
                  required:
                  - durationMinutes
                  - start
                  type: object
                type: array
            required:
            - clusterRef
            type: object
          status:
            description: ClusterMaintenanceStatus defines the observed state of ClusterMaintenance.
            properties:
              activeWindowName:
                description: |-
                  ActiveWindowName is the name of the currently active maintenance window,
                  or empty if no window is currently active.
                type: string
              conditions:
                description: |-
                  Conditions is the list of status conditions for this ClusterMaintenance.
                  Condition types: Paused, WindowActive, LineageSynced.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the generation of the spec last
                  reconciled.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: clusterresets.platform.ontai.dev
spec:
  group: platform.ontai.dev
  names:
    kind: ClusterReset
    listKind: ClusterResetList
    plural: clusterresets
    shortNames:
    - crst
    singular: clusterreset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .status.conditions[?(@.type=="PendingApproval")].status
      name: PendingApproval
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ClusterReset performs a destructive factory reset of a Talos cluster.

          HUMAN GATE REQUIRED: the ontai.dev/reset-approved=true annotation must be
          present on this object before any reconciliation proceeds. The reconciler
          holds at PendingApproval and emits an event if the annotation is absent.
          INV-007, CP-INV-006.

          For CAPI-managed clusters (capi.enabled=true): the CAPI Cluster object is
          deleted first, then all Machine objects are drained through the Seam
          Infrastructure Provider, then the cluster-reset Conductor Job is submitted.

          For management cluster (capi.enabled=false): the cluster-reset Conductor Job
          is submitted directly.

          Named Conductor capability: cluster-reset. platform-schema.md §5.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterResetSpec defines the desired state of ClusterReset.
            properties:
              clusterRef:
                description: |-
                  ClusterRef references the TalosCluster to reset. The cluster and its tenant
                  namespace will be factory reset and decommissioned.
                properties:
                  name:
                    description: Name is the object name.
                    type: string
                  namespace:
                    description: Namespace is the object namespace. May be empty for
                      cluster-scoped objects.
                    type: string
                required:
                - name
                type: object
              drainGracePeriodSeconds:
                description: |-
                  DrainGracePeriodSeconds is the number of seconds to wait for node drain to
                  complete before forcing the reset. Defaults to 300 if not set.
                format: int32
                type: integer
              lineage:
                description: |-
                  Lineage is the sealed causal chain record for this root declaration.
                  Authored once at object creation time and immutable thereafter.
                  seam-core-schema.md §5, CLAUDE.md §14 Decision 1.
                properties:
                  creatingOperator:
                    description: |-
                      CreatingOperator identifies the Seam Operator that created this object.
                      This is a structured identity carrying the operator name and its deployed
                      version at creation time.
                    properties:
                      name:
                        description: |-
                          Name is the canonical name of the Seam Operator (e.g., platform, guardian,
                          wrapper, conductor).
                        type: string
                      version:
                        description: |-
                          Version is the deployed version of the operator at the time the object was
                          created (e.g., v1.26.5-r3). This allows audit tooling to correlate objects
                          with the operator version that produced them.
                        type: string
                    required:
                    - name
                    - version
                    type: object
                  creationRationale:
                    description: |-
                      CreationRationale is the reason this object was created, drawn from the
                      Seam Core controlled vocabulary defined in rationale.go. It is not a
                      free-text field.
                    enum:
                    - ClusterProvision
                    - ClusterDecommission
                    - SecurityEnforcement
                    - PackExecution
                    - VirtualizationFulfillment
                    - ConductorAssignment
                    - VortexBinding
                    type: string
                  rootGenerationAtCreation:
                    description: |-
                      RootGenerationAtCreation is the metadata.generation of the root declaration
                      at the time this object was created. Together with RootUID, it provides a
                      complete temporal anchor for the derivation record.
                    format: int64
                    type: integer
                  rootKind:
                    description: |-
                      RootKind is the kind of the root declaration that caused this object to
                      exist (e.g., TalosCluster, PackExecution, RBACPolicy).
                    type: string
                  rootName:
                    description: RootName is the name of the root declaration.
                    type: string
                  rootNamespace:
                    description: RootNamespace is the namespace of the root declaration.
                    type: string
                  rootUID:
                    description: |-
                      RootUID is the UID of the root declaration at the time this object was
                      created. Used to verify that no root declaration replacement has occurred.
                    type: string
                required:
                - creatingOperator
                - creationRationale
                - rootGenerationAtCreation
                - rootKind
                - rootName
                - rootNamespace
                - rootUID
                type: object
              wipeDisks:
                description: |-
                  WipeDisks controls whether the Talos reset API is called with wipeDisks=true,
                  which destroys all data on the node's disks. Defaults to false.
                type: boolean
            required:
            - clusterRef
            type: object
          status:
            description: ClusterResetStatus defines the observed state of ClusterReset.
            properties:
              conditions:
                description: |-
                  Conditions is the list of status conditions for this ClusterReset.
                  Condition types: PendingApproval, Ready, Degraded, LineageSynced.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              jobName:
                description: JobName is the name of the Conductor executor Job submitted
                  for the reset.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the spec last
                  reconciled.
                format: int64
                type: integer
              operationResult:
                description: OperationResult is the message from the Conductor OperationResult
                  ConfigMap.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: etcdmaintenances.platform.ontai.dev
spec:
  group: platform.ontai.dev
  names:
    kind: EtcdMaintenance
    listKind: EtcdMaintenanceList
    plural: etcdmaintenances
    shortNames:
    - em
    singular: etcdmaintenance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.operation
      name: Operation
      type: string
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          EtcdMaintenance covers all etcd lifecycle operations for both management and
          target clusters. CAPI has no etcd concept — this always submits a direct
          Conductor executor Job regardless of the owning TalosCluster's capi.enabled.
          Named Conductor capabilities: etcd-backup, etcd-restore, etcd-defrag.
          platform-schema.md §5.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EtcdMaintenanceSpec defines the desired state of EtcdMaintenance.
            properties:
              clusterRef:
                description: |-
                  ClusterRef references the TalosCluster this operation targets.
                  The TalosCluster must exist in ont-system (management cluster) or in the
                  tenant namespace (target cluster).
                properties:
                  name:
                    description: Name is the object name.
                    type: string
                  namespace:
                    description: Namespace is the object namespace. May be empty for
                      cluster-scoped objects.
                    type: string
                required:
                - name
                type: object
              etcdBackupS3SecretRef:
                description: |-
                  EtcdBackupS3SecretRef references a Secret containing S3 backup destination
                  configuration for this operation. Takes precedence over the cluster-wide
                  seam-etcd-backup-config Secret in seam-system. Required when operation=backup
                  and no cluster default is configured. platform-schema.md §10.
                properties:
                  name:
                    description: name is unique within a namespace to reference a
                      secret resource.
                    type: string
                  namespace:
                    description: namespace defines the space within which the secret
                      name must be unique.
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              lineage:
                description: |-
                  Lineage is the sealed causal chain record for this root declaration.
                  Authored once at object creation time and immutable thereafter.
                  seam-core-schema.md §5, CLAUDE.md §14 Decision 1.
                properties:
                  creatingOperator:
                    description: |-
                      CreatingOperator identifies the Seam Operator that created this object.
                      This is a structured identity carrying the operator name and its deployed
                      version at creation time.
                    properties:
                      name:
                        description: |-
                          Name is the canonical name of the Seam Operator (e.g., platform, guardian,
                          wrapper, conductor).
                        type: string
                      version:
                        description: |-
                          Version is the deployed version of the operator at the time the object was
                          created (e.g., v1.26.5-r3). This allows audit tooling to correlate objects
                          with the operator version that produced them.
                        type: string
                    required:
                    - name
                    - version
                    type: object
                  creationRationale:
                    description: |-
                      CreationRationale is the reason this object was created, drawn from the
                      Seam Core controlled vocabulary defined in rationale.go. It is not a
                      free-text field.
                    enum:
                    - ClusterProvision
                    - ClusterDecommission
                    - SecurityEnforcement
                    - PackExecution
                    - VirtualizationFulfillment
                    - ConductorAssignment
                    - VortexBinding
                    type: string
                  rootGenerationAtCreation:
                    description: |-
                      RootGenerationAtCreation is the metadata.generation of the root declaration
                      at the time this object was created. Together with RootUID, it provides a
                      complete temporal anchor for the derivation record.
                    format: int64
                    type: integer
                  rootKind:
                    description: |-
                      RootKind is the kind of the root declaration that caused this object to
                      exist (e.g., TalosCluster, PackExecution, RBACPolicy).
                    type: string
                  rootName:
                    description: RootName is the name of the root declaration.
                    type: string
                  rootNamespace:
                    description: RootNamespace is the namespace of the root declaration.
                    type: string
                  rootUID:
                    description: |-
                      RootUID is the UID of the root declaration at the time this object was
                      created. Used to verify that no root declaration replacement has occurred.
                    type: string
                required:
                - creatingOperator
                - creationRationale
                - rootGenerationAtCreation
                - rootKind
                - rootName
                - rootNamespace
                - rootUID
                type: object
              operation:
                allOf:
                - enum:
                  - backup
                  - restore
                  - defrag
                - enum:
                  - backup
                  - restore
                  - defrag
                description: |-
                  Operation declares the etcd lifecycle operation to perform.
                  Named Conductor capabilities: etcd-backup, etcd-restore, etcd-defrag.
                  platform-schema.md §5 EtcdMaintenance.
                type: string
              s3Destination:
                description: |-
                  S3Destination is the S3 location to write the etcd snapshot to.
                  Required when operation=backup.
                properties:
                  bucket:
                    description: Bucket is the S3 bucket name.
                    type: string
                  credentialsSecretRef:
                    description: |-
                      CredentialsSecretRef references the Secret containing S3 credentials.
                      The Secret must be in ont-system.
                    properties:
                      name:
                        description: Name is the Secret name.
                        type: string
                      namespace:
                        description: |-
                          Namespace is the Secret namespace. When empty, the consuming object's
                          own namespace is used unless the schema specifies otherwise.
                        type: string
                    required:
                    - name
                    type: object
                  key:
                    description: Key is the S3 object key path.
                    type: string
                required:
                - bucket
                - credentialsSecretRef
                - key
                type: object
              s3SnapshotPath:
                description: |-
                  S3SnapshotPath is the S3 location of the snapshot to restore from.
                  Required when operation=restore.
                properties:
                  bucket:
                    description: Bucket is the S3 bucket name.
                    type: string
                  credentialsSecretRef:
                    description: |-
                      CredentialsSecretRef references the Secret containing S3 credentials.
                      The Secret must be in ont-system.
                    properties:
                      name:
                        description: Name is the Secret name.
                        type: string
                      namespace:
                        description: |-
                          Namespace is the Secret namespace. When empty, the consuming object's
                          own namespace is used unless the schema specifies otherwise.
                        type: string
                    required:
                    - name
                    type: object
                  key:
                    description: Key is the S3 object key path.
                    type: string
                required:
                - bucket
                - credentialsSecretRef
                - key
                type: object
              schedule:
                description: |-
                  Schedule is a cron expression for recurring backup operations.
                  When set with operation=backup, a recurring Job is submitted on schedule.
                type: string
              targetNodes:
                description: |-
                  TargetNodes is the list of node names to target for restore operations.
                  When empty for restore, all etcd members are targeted.
                items:
                  type: string
                type: array
            required:
            - clusterRef
            - operation
            type: object
          status:
            description: EtcdMaintenanceStatus defines the observed state of EtcdMaintenance.
            properties:
              conditions:
                description: |-
                  Conditions is the list of status conditions for this EtcdMaintenance.
                  Condition types: Ready, Running, Degraded, LineageSynced.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              jobName:
                description: JobName is the name of the most recently submitted Conductor
                  executor Job.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the spec last
                  reconciled.
                format: int64
                type: integer
              operationResult:
                description: OperationResult is the message from the Conductor OperationResult
                  ConfigMap.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: hardeningprofiles.platform.ontai.dev
spec:
  group: platform.ontai.dev
  names:
    kind: HardeningProfile
    listKind: HardeningProfileList
    plural: hardeningprofiles
    shortNames:
    - hp
    singular: hardeningprofile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          HardeningProfile is a reusable hardening ruleset referenced by NodeMaintenance
          at runtime (operation=hardening-apply) and by TalosControlPlane and
          TalosWorkerConfig at compile time. It is a configuration CR only — it does
          not directly trigger a Conductor Job. Jobs are submitted by NodeMaintenance
          when it references this profile. platform-schema.md §5.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HardeningProfileSpec defines the desired state of HardeningProfile.
            properties:
              description:
                description: Description is a human-readable description of this hardening
                  profile.
                type: string
              lineage:
                description: |-
                  Lineage is the sealed causal chain record for this root declaration.
                  Authored once at object creation time and immutable thereafter.
                  seam-core-schema.md §5, CLAUDE.md §14 Decision 1.
                properties:
                  creatingOperator:
                    description: |-
                      CreatingOperator identifies the Seam Operator that created this object.
                      This is a structured identity carrying the operator name and its deployed
                      version at creation time.
                    properties:
                      name:
                        description: |-
                          Name is the canonical name of the Seam Operator (e.g., platform, guardian,
                          wrapper, conductor).
                        type: string
                      version:
                        description: |-
                          Version is the deployed version of the operator at the time the object was
                          created (e.g., v1.26.5-r3). This allows audit tooling to correlate objects
                          with the operator version that produced them.
                        type: string
                    required:
                    - name
                    - version
                    type: object
                  creationRationale:
                    description: |-
                      CreationRationale is the reason this object was created, drawn from the
                      Seam Core controlled vocabulary defined in rationale.go. It is not a
                      free-text field.
                    enum:
                    - ClusterProvision
                    - ClusterDecommission
                    - SecurityEnforcement
                    - PackExecution
                    - VirtualizationFulfillment
                    - ConductorAssignment
                    - VortexBinding
                    type: string
                  rootGenerationAtCreation:
                    description: |-
                      RootGenerationAtCreation is the metadata.generation of the root declaration
                      at the time this object was created. Together with RootUID, it provides a
                      complete temporal anchor for the derivation record.
                    format: int64
                    type: integer
                  rootKind:
                    description: |-
                      RootKind is the kind of the root declaration that caused this object to
                      exist (e.g., TalosCluster, PackExecution, RBACPolicy).
                    type: string
                  rootName:
                    description: RootName is the name of the root declaration.
                    type: string
                  rootNamespace:
                    description: RootNamespace is the namespace of the root declaration.
                    type: string
                  rootUID:
                    description: |-
                      RootUID is the UID of the root declaration at the time this object was
                      created. Used to verify that no root declaration replacement has occurred.
                    type: string
                required:
                - creatingOperator
                - creationRationale
                - rootGenerationAtCreation
                - rootKind
                - rootName
                - rootNamespace
                - rootUID
                type: object
              machineConfigPatches:
                description: |-
                  MachineConfigPatches is the list of Talos machine config patches to apply
                  when this profile is used. Patches are expressed as JSON Patch operations
                  applied to the rendered machineconfig. At compile time, Compiler merges
                  these patches into TalosConfigTemplate. At runtime, NodeMaintenance
                  operation=hardening-apply submits a Conductor Job to apply them.
                items:
                  type: string
                type: array
              sysctlParams:
                additionalProperties:
                  type: string
                description: |-
                  SysctlParams is a map of sysctl key/value pairs to apply to target nodes.
                  Merged into the machineconfig sysctl section.
                type: object
            type: object
          status:
            description: HardeningProfileStatus defines the observed state of HardeningProfile.
            properties:
              conditions:
                description: |-
                  Conditions is the list of status conditions for this HardeningProfile.
                  Condition types: LineageSynced.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the generation of the spec last
                  reconciled.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: maintenancebundles.platform.ontai.dev
spec:
  group: platform.ontai.dev
  names:
    kind: MaintenanceBundle
    listKind: MaintenanceBundleList
    plural: maintenancebundles
    shortNames:
    - mb
    singular: maintenancebundle
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.operation
      name: Operation
      type: string
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          MaintenanceBundle is a pre-compiled scheduling artifact produced by
          `compiler maintenance`. Carries pre-resolved scheduling context so neither
          Platform nor Conductor need to perform cluster queries at execution time.
          conductor-schema.md §9. API group: platform.ontai.dev.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              MaintenanceBundleSpec defines the desired state of MaintenanceBundle.
              All fields are pre-resolved by `compiler maintenance` at compile time.
              conductor-schema.md §9.
            properties:
              clusterRef:
                description: ClusterRef references the TalosCluster this bundle targets.
                properties:
                  name:
                    description: Name is the object name.
                    type: string
                  namespace:
                    description: Namespace is the object namespace. May be empty for
                      cluster-scoped objects.
                    type: string
                required:
                - name
                type: object
              lineage:
                description: |-
                  Lineage is the sealed causal chain record for this root declaration.
                  Authored once at object creation time and immutable thereafter.
                  seam-core-schema.md §5, CLAUDE.md §14 Decision 1.
                properties:
                  creatingOperator:
                    description: |-
                      CreatingOperator identifies the Seam Operator that created this object.
                      This is a structured identity carrying the operator name and its deployed
                      version at creation time.
                    properties:
                      name:
                        description: |-
                          Name is the canonical name of the Seam Operator (e.g., platform, guardian,
                          wrapper, conductor).
                        type: string
                      version:
                        description: |-
                          Version is the deployed version of the operator at the time the object was
                          created (e.g., v1.26.5-r3). This allows audit tooling to correlate objects
                          with the operator version that produced them.
                        type: string
                    required:
                    - name
                    - version
                    type: object
                  creationRationale:
                    description: |-
                      CreationRationale is the reason this object was created, drawn from the
                      Seam Core controlled vocabulary defined in rationale.go. It is not a
                      free-text field.
                    enum:
                    - ClusterProvision
                    - ClusterDecommission
                    - SecurityEnforcement
                    - PackExecution
                    - VirtualizationFulfillment
                    - ConductorAssignment
                    - VortexBinding
                    type: string
                  rootGenerationAtCreation:
                    description: |-
                      RootGenerationAtCreation is the metadata.generation of the root declaration
                      at the time this object was created. Together with RootUID, it provides a
                      complete temporal anchor for the derivation record.
                    format: int64
                    type: integer
                  rootKind:
                    description: |-
                      RootKind is the kind of the root declaration that caused this object to
                      exist (e.g., TalosCluster, PackExecution, RBACPolicy).
                    type: string
                  rootName:
                    description: RootName is the name of the root declaration.
                    type: string
                  rootNamespace:
                    description: RootNamespace is the namespace of the root declaration.
                    type: string
                  rootUID:
                    description: |-
                      RootUID is the UID of the root declaration at the time this object was
                      created. Used to verify that no root declaration replacement has occurred.
                    type: string
                required:
                - creatingOperator
                - creationRationale
                - rootGenerationAtCreation
                - rootKind
                - rootName
                - rootNamespace
                - rootUID
                type: object
              maintenanceTargetNodes:
                description: |-
                  MaintenanceTargetNodes is the pre-resolved list of nodes targeted by the
                  operation. Validated against the live cluster by `compiler maintenance`.
                  Directly populates RunnerConfig.MaintenanceTargetNodes at execution time.
                items:
                  type: string
                type: array
              operation:
                allOf:
                - enum:
                  - drain
                  - upgrade
                  - etcd-backup
                  - machineconfig-rotation
                - enum:
                  - drain
                  - upgrade
                  - etcd-backup
                  - machineconfig-rotation
                description: |-
                  Operation declares the maintenance operation type.
                  Determines which Conductor capability the resulting RunnerConfig targets.
                  conductor-schema.md §9.
                type: string
              operatorLeaderNode:
                description: |-
                  OperatorLeaderNode is the node hosting the leader pod of the Platform operator
                  at compile time, resolved from the platform-leader Lease in seam-system.
                  Directly populates RunnerConfig.OperatorLeaderNode at execution time.
                  conductor-schema.md §13.
                type: string
              s3ConfigSecretRef:
                description: |-
                  S3ConfigSecretRef references the S3 configuration Secret, pre-resolved and
                  validated to exist by `compiler maintenance`. Follows the platform-schema.md §10
                  S3 resolution hierarchy. A MaintenanceBundle is never committed without a valid
                  S3 reference when the operation requires it (etcd-backup).
                properties:
                  name:
                    description: name is unique within a namespace to reference a
                      secret resource.
                    type: string
                  namespace:
                    description: namespace defines the space within which the secret
                      name must be unique.
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - clusterRef
            - operation
            type: object
          status:
            description: MaintenanceBundleStatus defines the observed state of MaintenanceBundle.
            properties:
              conditions:
                description: |-
                  Conditions is the list of status conditions for this MaintenanceBundle.
                  Condition types: Ready, Pending, Degraded, LineageSynced.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              jobName:
                description: JobName is the name of the Conductor executor Job submitted
                  for this bundle.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the spec last
                  reconciled.
                format: int64
                type: integer
              operationResult:
                description: OperationResult carries the result message written by
                  the Conductor executor Job.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: nodemaintenances.platform.ontai.dev
spec:
  group: platform.ontai.dev
  names:
    kind: NodeMaintenance
    listKind: NodeMaintenanceList
    plural: nodemaintenances
    shortNames:
    - nm
    singular: nodemaintenance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.operation
      name: Operation
      type: string
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          NodeMaintenance covers targeted node-level operations CAPI has no equivalent
          for. Applies to both management and target clusters via a direct Conductor
          executor Job regardless of the owning TalosCluster's capi.enabled value.
          Named Conductor capabilities: node-patch, hardening-apply, credential-rotate.
          platform-schema.md §5.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NodeMaintenanceSpec defines the desired state of NodeMaintenance.
            properties:
              clusterRef:
                description: ClusterRef references the TalosCluster this operation
                  targets.
                properties:
                  name:
                    description: Name is the object name.
                    type: string
                  namespace:
                    description: Namespace is the object namespace. May be empty for
                      cluster-scoped objects.
                    type: string
                required:
                - name
                type: object
              hardeningProfileRef:
                description: |-
                  HardeningProfileRef references the HardeningProfile CR to apply.
                  Required when operation=hardening-apply.
                properties:
                  name:
                    description: Name is the object name.
                    type: string
                  namespace:
                    description: Namespace is the object namespace. May be empty for
                      cluster-scoped objects.
                    type: string
                required:
                - name
                type: object
              lineage:
                description: |-
                  Lineage is the sealed causal chain record for this root declaration.
                  Authored once at object creation time and immutable thereafter.
                  seam-core-schema.md §5, CLAUDE.md §14 Decision 1.
                properties:
                  creatingOperator:
                    description: |-
                      CreatingOperator identifies the Seam Operator that created this object.
                      This is a structured identity carrying the operator name and its deployed
                      version at creation time.
                    properties:
                      name:
                        description: |-
                          Name is the canonical name of the Seam Operator (e.g., platform, guardian,
                          wrapper, conductor).
                        type: string
                      version:
                        description: |-
                          Version is the deployed version of the operator at the time the object was
                          created (e.g., v1.26.5-r3). This allows audit tooling to correlate objects
                          with the operator version that produced them.
                        type: string
                    required:
                    - name
                    - version
                    type: object
                  creationRationale:
                    description: |-
                      CreationRationale is the reason this object was created, drawn from the
                      Seam Core controlled vocabulary defined in rationale.go. It is not a
                      free-text field.
                    enum:
                    - ClusterProvision
                    - ClusterDecommission
                    - SecurityEnforcement
                    - PackExecution
                    - VirtualizationFulfillment
                    - ConductorAssignment
                    - VortexBinding
                    type: string
                  rootGenerationAtCreation:
                    description: |-
                      RootGenerationAtCreation is the metadata.generation of the root declaration
                      at the time this object was created. Together with RootUID, it provides a
                      complete temporal anchor for the derivation record.
                    format: int64
                    type: integer
                  rootKind:
                    description: |-
                      RootKind is the kind of the root declaration that caused this object to
                      exist (e.g., TalosCluster, PackExecution, RBACPolicy).
                    type: string
                  rootName:
                    description: RootName is the name of the root declaration.
                    type: string
                  rootNamespace:
                    description: RootNamespace is the namespace of the root declaration.
                    type: string
                  rootUID:
                    description: |-
                      RootUID is the UID of the root declaration at the time this object was
                      created. Used to verify that no root declaration replacement has occurred.
                    type: string
                required:
                - creatingOperator
                - creationRationale
                - rootGenerationAtCreation
                - rootKind
                - rootName
                - rootNamespace
                - rootUID
                type: object
              operation:
                allOf:
                - enum:
                  - patch
                  - hardening-apply
                  - credential-rotate
                - enum:
                  - patch
                  - hardening-apply
                  - credential-rotate
                description: |-
                  Operation declares the node-level operation to perform.
                  Named Conductor capabilities: node-patch, hardening-apply, credential-rotate.
                  platform-schema.md §5 NodeMaintenance.
                type: string
              patchSecretRef:
                description: |-
                  PatchSecretRef references the Secret containing the machine config patch YAML.
                  Required when operation=patch.
                properties:
                  name:
                    description: Name is the Secret name.
                    type: string
                  namespace:
                    description: |-
                      Namespace is the Secret namespace. When empty, the consuming object's
                      own namespace is used unless the schema specifies otherwise.
                    type: string
                required:
                - name
                type: object
              rotateOIDCCredentials:
                description: |-
                  RotateOIDCCredentials controls whether OIDC credentials are rotated.
                  Applies when operation=credential-rotate.
                type: boolean
              rotateServiceAccountKeys:
                description: |-
                  RotateServiceAccountKeys controls whether service account signing keys are
                  rotated. Applies when operation=credential-rotate.
                type: boolean
              targetNodes:
                description: |-
                  TargetNodes is the list of node names or IPs to target.
                  When empty, the operation targets all nodes in the cluster.
                items:
                  type: string
                type: array
            required:
            - clusterRef
            - operation
            type: object
          status:
            description: NodeMaintenanceStatus defines the observed state of NodeMaintenance.
            properties:
              conditions:
                description: |-
                  Conditions is the list of status conditions for this NodeMaintenance.
                  Condition types: Ready, Degraded, LineageSynced.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              jobName:
                description: JobName is the name of the most recently submitted Conductor
                  executor Job.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the spec last
                  reconciled.
                format: int64
                type: integer
              operationResult:
                description: OperationResult is the message from the Conductor OperationResult
                  ConfigMap.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: nodeoperations.platform.ontai.dev
spec:
  group: platform.ontai.dev
  names:
    kind: NodeOperation
    listKind: NodeOperationList
    plural: nodeoperations
    shortNames:
    - nop
    singular: nodeoperation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.operation
      name: Operation
      type: string
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          NodeOperation governs node lifecycle operations: scale-up, decommission, reboot.

          Dual-path CRD governed by spec.capi.enabled on the owning TalosCluster:
            - For CAPI-managed clusters (capi.enabled=true): modifies MachineDeployment
              replicas for scale-up, deletes specific Machine objects for decommission,
              or sets the Machine reboot annotation — all handled natively by CAPI.
            - For management cluster (capi.enabled=false): submits node-scale-up,
              node-decommission, or node-reboot Conductor executor Job.

          Named Conductor capabilities (non-CAPI path): node-scale-up, node-decommission,
          node-reboot. platform-schema.md §5.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NodeOperationSpec defines the desired state of NodeOperation.
            properties:
              clusterRef:
                description: ClusterRef references the TalosCluster this operation
                  targets.
                properties:
                  name:
                    description: Name is the object name.
                    type: string
                  namespace:
                    description: Namespace is the object namespace. May be empty for
                      cluster-scoped objects.
                    type: string
                required:
                - name
                type: object
              lineage:
                description: |-
                  Lineage is the sealed causal chain record for this root declaration.
                  Authored once at object creation time and immutable thereafter.
                  seam-core-schema.md §5, CLAUDE.md §14 Decision 1.
                properties:
                  creatingOperator:
                    description: |-
                      CreatingOperator identifies the Seam Operator that created this object.
                      This is a structured identity carrying the operator name and its deployed
                      version at creation time.
                    properties:
                      name:
                        description: |-
                          Name is the canonical name of the Seam Operator (e.g., platform, guardian,
                          wrapper, conductor).
                        type: string
                      version:
                        description: |-
                          Version is the deployed version of the operator at the time the object was
                          created (e.g., v1.26.5-r3). This allows audit tooling to correlate objects
                          with the operator version that produced them.
                        type: string
                    required:
                    - name
                    - version
                    type: object
                  creationRationale:
                    description: |-
                      CreationRationale is the reason this object was created, drawn from the
                      Seam Core controlled vocabulary defined in rationale.go. It is not a
                      free-text field.
                    enum:
                    - ClusterProvision
                    - ClusterDecommission
                    - SecurityEnforcement
                    - PackExecution
                    - VirtualizationFulfillment
                    - ConductorAssignment
                    - VortexBinding
                    type: string
                  rootGenerationAtCreation:
                    description: |-
                      RootGenerationAtCreation is the metadata.generation of the root declaration
                      at the time this object was created. Together with RootUID, it provides a
                      complete temporal anchor for the derivation record.
                    format: int64
                    type: integer
                  rootKind:
                    description: |-
                      RootKind is the kind of the root declaration that caused this object to
                      exist (e.g., TalosCluster, PackExecution, RBACPolicy).
                    type: string
                  rootName:
                    description: RootName is the name of the root declaration.
                    type: string
                  rootNamespace:
                    description: RootNamespace is the namespace of the root declaration.
                    type: string
                  rootUID:
                    description: |-
                      RootUID is the UID of the root declaration at the time this object was
                      created. Used to verify that no root declaration replacement has occurred.
                    type: string
                required:
                - creatingOperator
                - creationRationale
                - rootGenerationAtCreation
                - rootKind
                - rootName
                - rootNamespace
                - rootUID
                type: object
              operation:
                allOf:
                - enum:
                  - scale-up
                  - decommission
                  - reboot
                - enum:
                  - scale-up
                  - decommission
                  - reboot
                description: Operation declares the node lifecycle operation to perform.
                type: string
              replicaCount:
                description: |-
                  ReplicaCount is the desired number of worker replicas after scale-up.
                  Required when operation=scale-up.
                format: int32
                type: integer
              targetNodes:
                description: |-
                  TargetNodes is the list of node names to target for decommission or reboot.
                  Required when operation=decommission or operation=reboot.
                items:
                  type: string
                type: array
            required:
            - clusterRef
            - operation
            type: object
          status:
            description: NodeOperationStatus defines the observed state of NodeOperation.
            properties:
              conditions:
                description: |-
                  Conditions is the list of status conditions for this NodeOperation.
                  Condition types: Ready, Degraded, CAPIDelegated, LineageSynced.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              jobName:
                description: |-
                  JobName is the name of the Conductor executor Job submitted for this operation.
                  Only set for the capi.enabled=false (non-CAPI) path.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the spec last
                  reconciled.
                format: int64
                type: integer
              operationResult:
                description: OperationResult is the message from the Conductor OperationResult
                  ConfigMap.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: pkirotations.platform.ontai.dev
spec:
  group: platform.ontai.dev
  names:
    kind: PKIRotation
    listKind: PKIRotationList
    plural: pkirotations
    shortNames:
    - pkir
    singular: pkirotation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          PKIRotation triggers a cluster PKI rotation for either the management cluster
          or a target cluster. Always submits a direct Conductor executor Job via the
          pki-rotate named capability regardless of the owning TalosCluster's
          capi.enabled value. CAPI has no PKI rotation equivalent.
          Named Conductor capability: pki-rotate. platform-schema.md §5.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PKIRotationSpec defines the desired state of PKIRotation.
            properties:
              clusterRef:
                description: |-
                  ClusterRef references the TalosCluster whose PKI is to be rotated.
                  Applies to both management and target clusters.
                properties:
                  name:
                    description: Name is the object name.
                    type: string
                  namespace:
                    description: Namespace is the object namespace. May be empty for
                      cluster-scoped objects.
                    type: string
                required:
                - name
                type: object
              lineage:
                description: |-
                  Lineage is the sealed causal chain record for this root declaration.
                  Authored once at object creation time and immutable thereafter.
                  seam-core-schema.md §5, CLAUDE.md §14 Decision 1.
                properties:
                  creatingOperator:
                    description: |-
                      CreatingOperator identifies the Seam Operator that created this object.
                      This is a structured identity carrying the operator name and its deployed
                      version at creation time.
                    properties:
                      name:
                        description: |-
                          Name is the canonical name of the Seam Operator (e.g., platform, guardian,
                          wrapper, conductor).
                        type: string
                      version:
                        description: |-
                          Version is the deployed version of the operator at the time the object was
                          created (e.g., v1.26.5-r3). This allows audit tooling to correlate objects
                          with the operator version that produced them.
                        type: string
                    required:
                    - name
                    - version
                    type: object
                  creationRationale:
                    description: |-
                      CreationRationale is the reason this object was created, drawn from the
                      Seam Core controlled vocabulary defined in rationale.go. It is not a
                      free-text field.
                    enum:
                    - ClusterProvision
                    - ClusterDecommission
                    - SecurityEnforcement
                    - PackExecution
                    - VirtualizationFulfillment
                    - ConductorAssignment
                    - VortexBinding
                    type: string
                  rootGenerationAtCreation:
                    description: |-
                      RootGenerationAtCreation is the metadata.generation of the root declaration
                      at the time this object was created. Together with RootUID, it provides a
                      complete temporal anchor for the derivation record.
                    format: int64
                    type: integer
                  rootKind:
                    description: |-
                      RootKind is the kind of the root declaration that caused this object to
                      exist (e.g., TalosCluster, PackExecution, RBACPolicy).
                    type: string
                  rootName:
                    description: RootName is the name of the root declaration.
                    type: string
                  rootNamespace:
                    description: RootNamespace is the namespace of the root declaration.
                    type: string
                  rootUID:
                    description: |-
                      RootUID is the UID of the root declaration at the time this object was
                      created. Used to verify that no root declaration replacement has occurred.
                    type: string
                required:
                - creatingOperator
                - creationRationale
                - rootGenerationAtCreation
                - rootKind
                - rootName
                - rootNamespace
                - rootUID
                type: object
            required:
            - clusterRef
            type: object
          status:
            description: PKIRotationStatus defines the observed state of PKIRotation.
            properties:
              conditions:
                description: |-
                  Conditions is the list of status conditions for this PKIRotation.
                  Condition types: Ready, Degraded, LineageSynced.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              jobName:
                description: JobName is the name of the Conductor executor Job submitted
                  for this rotation.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the spec last
                  reconciled.
                format: int64
                type: integer
              operationResult:
                description: OperationResult is the message from the Conductor OperationResult
                  ConfigMap.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: talosclusters.platform.ontai.dev
spec:
  group: platform.ontai.dev
  names:
    kind: TalosCluster
    listKind: TalosClusterList
    plural: talosclusters
    shortNames:
    - tc
    singular: taloscluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.mode
      name: Mode
      type: string
    - jsonPath: .spec.capi.enabled
      name: CAPI
      type: boolean
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          TalosCluster is the Seam root CR for every cluster. For target clusters it
          owns all CAPI objects as children. For the management cluster it is the
          bootstrap record and operational anchor. platform-schema.md §5.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TalosClusterSpec defines the desired state of a TalosCluster.
            properties:
              capi:
                description: |-
                  CAPI holds the CAPI-specific configuration for target cluster lifecycle.
                  When CAPI.Enabled=false (management cluster), this field is ignored except
                  for the Enabled flag itself.
                properties:
                  ciliumPackRef:
                    description: |-
                      CiliumPackRef references the cluster-specific Cilium ClusterPack.
                      Applied as the first pack after the CAPI cluster reaches Running state.
                      Required when Enabled=true. platform-schema.md §2.3.
                    properties:
                      name:
                        description: Name is the ClusterPack CR name for the Cilium
                          pack.
                        type: string
                      version:
                        description: Version is the ClusterPack version string.
                        type: string
                    required:
                    - name
                    - version
                    type: object
                  controlPlane:
                    description: ControlPlane holds control plane configuration. Required
                      when Enabled=true.
                    properties:
                      replicas:
                        description: Replicas is the desired number of control plane
                          nodes.
                        format: int32
                        type: integer
                    type: object
                  enabled:
                    description: |-
                      Enabled determines whether this TalosCluster uses the CAPI path.
                      True for all target clusters. False for the management cluster.
                      When true, the TalosClusterReconciler creates and owns CAPI objects.
                      When false, it follows the direct bootstrap path via Conductor Job.
                    type: boolean
                  kubernetesVersion:
                    description: |-
                      KubernetesVersion is the Kubernetes version for TalosControlPlane.
                      Required when Enabled=true.
                    type: string
                  talosVersion:
                    description: |-
                      TalosVersion is the Talos version to use for TalosConfigTemplate and
                      CABPT machineconfig generation. Required when Enabled=true.
                    type: string
                  workers:
                    description: |-
                      Workers is the list of worker node pools. Each pool maps to a
                      MachineDeployment + SeamInfrastructureMachineTemplate.
                    items:
                      description: |-
                        CAPIWorkerPool declares a worker node pool for a CAPI-managed target cluster.
                        Each pool maps to a MachineDeployment + SeamInfrastructureMachineTemplate.
                      properties:
                        name:
                          description: Name is the pool identifier. Used as the MachineDeployment
                            name suffix.
                          type: string
                        replicas:
                          description: Replicas is the desired number of worker nodes
                            in this pool.
                          format: int32
                          type: integer
                        seamInfrastructureMachineNames:
                          description: |-
                            SeamInfrastructureMachineNames lists the SeamInfrastructureMachine CR names
                            pre-provisioned for this pool. One per node. The Seam Infrastructure Provider
                            delivers machineconfigs to these nodes when CABPT renders their configs.
                          items:
                            type: string
                          type: array
                      required:
                      - name
                      type: object
                    type: array
                required:
                - enabled
                type: object
              clusterEndpoint:
                description: |-
                  ClusterEndpoint is the cluster VIP or primary API endpoint IP used to
                  reach the control plane. Required on the import path when generating the
                  kubeconfig Secret from the talosconfig Secret. Optional for bootstrap mode
                  (endpoint is derived from the bootstrap Job output). platform-schema.md §5.
                type: string
              infrastructureProvider:
                allOf:
                - enum:
                  - native
                  - capi
                  - screen
                - enum:
                  - native
                  - capi
                  - screen
                default: native
                description: |-
                  InfrastructureProvider declares the infrastructure provider backing this cluster.
                  Defaults to native when absent. The only reserved future value is screen (INV-021):
                  when set to screen the reconciler surfaces ScreenProviderNotImplemented and halts.
                type: string
              lineage:
                description: |-
                  Lineage is the sealed causal chain record for this root declaration.
                  Authored once at object creation time and immutable thereafter.
                  The admission webhook rejects any update that modifies this field after creation.
                  seam-core-schema.md §5, CLAUDE.md §14 Decision 1.
                properties:
                  creatingOperator:
                    description: |-
                      CreatingOperator identifies the Seam Operator that created this object.
                      This is a structured identity carrying the operator name and its deployed
                      version at creation time.
                    properties:
                      name:
                        description: |-
                          Name is the canonical name of the Seam Operator (e.g., platform, guardian,
                          wrapper, conductor).
                        type: string
                      version:
                        description: |-
                          Version is the deployed version of the operator at the time the object was
                          created (e.g., v1.26.5-r3). This allows audit tooling to correlate objects
                          with the operator version that produced them.
                        type: string
                    required:
                    - name
                    - version
                    type: object
                  creationRationale:
                    description: |-
                      CreationRationale is the reason this object was created, drawn from the
                      Seam Core controlled vocabulary defined in rationale.go. It is not a
                      free-text field.
                    enum:
                    - ClusterProvision
                    - ClusterDecommission
                    - SecurityEnforcement
                    - PackExecution
                    - VirtualizationFulfillment
                    - ConductorAssignment
                    - VortexBinding
                    type: string
                  rootGenerationAtCreation:
                    description: |-
                      RootGenerationAtCreation is the metadata.generation of the root declaration
                      at the time this object was created. Together with RootUID, it provides a
                      complete temporal anchor for the derivation record.
                    format: int64
                    type: integer
                  rootKind:
                    description: |-
                      RootKind is the kind of the root declaration that caused this object to
                      exist (e.g., TalosCluster, PackExecution, RBACPolicy).
                    type: string
                  rootName:
                    description: RootName is the name of the root declaration.
                    type: string
                  rootNamespace:
                    description: RootNamespace is the namespace of the root declaration.
                    type: string
                  rootUID:
                    description: |-
                      RootUID is the UID of the root declaration at the time this object was
                      created. Used to verify that no root declaration replacement has occurred.
                    type: string
                required:
                - creatingOperator
                - creationRationale
                - rootGenerationAtCreation
                - rootKind
                - rootName
                - rootNamespace
                - rootUID
                type: object
              mode:
                allOf:
                - enum:
                  - bootstrap
                  - import
                - enum:
                  - bootstrap
                  - import
                description: |-
                  Mode declares whether this TalosCluster bootstraps a new cluster or
                  imports an existing one. platform-schema.md §5.
                type: string
              nodeAddresses:
                description: |-
                  NodeAddresses is the list of node IPs belonging to this cluster.
                  Used by DSNSReconciler to populate A records in the seam DNS zone.
                  Optional. platform-schema.md §5.
                items:
                  type: string
                type: array
              role:
                allOf:
                - enum:
                  - management
                  - tenant
                - enum:
                  - management
                  - tenant
                description: |-
                  Role declares whether this TalosCluster is the Seam management cluster or a
                  tenant (target) cluster. Required on the direct bootstrap path (capi.enabled=false)
                  to determine namespace creation and kubeconfig routing. When absent, the
                  reconciler defaults to management behaviour (no seam-tenant namespace).
                  platform-schema.md §5. WS5.
                type: string
              talosVersion:
                description: |-
                  TalosVersion is the Talos version running on this cluster (e.g. "v1.9.3").
                  Required for management clusters (capi.enabled=false): the conductor image is
                  derived from this field as {registry}/conductor:{talosVersion}-dev.
                  conductor-schema.md §3, INV-012.
                type: string
            required:
            - mode
            type: object
          status:
            description: TalosClusterStatus defines the observed state of a TalosCluster.
            properties:
              capiClusterRef:
                description: |-
                  CAPIClusterRef is a reference to the owned CAPI Cluster object in the tenant
                  namespace. Only set for CAPI-managed clusters (capi.enabled=true).
                properties:
                  name:
                    description: Name is the object name.
                    type: string
                  namespace:
                    description: Namespace is the object namespace. May be empty for
                      cluster-scoped objects.
                    type: string
                required:
                - name
                type: object
              conditions:
                description: |-
                  Conditions is the list of status conditions for this TalosCluster.
                  Condition types: Ready, Bootstrapping, Importing, Degraded,
                  CiliumPending, LineageSynced.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the generation of the TalosCluster
                  spec last reconciled.
                format: int64
                type: integer
              origin:
                description: Origin records how the cluster came to exist.
                enum:
                - bootstrapped
                - imported
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: upgradepolicies.platform.ontai.dev
spec:
  group: platform.ontai.dev
  names:
    kind: UpgradePolicy
    listKind: UpgradePolicyList
    plural: upgradepolicies
    shortNames:
    - upgp
    singular: upgradepolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.upgradeType
      name: Type
      type: string
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          UpgradePolicy governs Talos OS, Kubernetes, or combined stack upgrades.

          Dual-path CRD governed by spec.capi.enabled on the owning TalosCluster:
            - For CAPI-managed clusters (capi.enabled=true): updates TalosControlPlane
              version and MachineDeployment rolling upgrade settings natively through
              CAPI machinery. No Conductor Job is submitted.
            - For management cluster (capi.enabled=false): submits talos-upgrade,
              kube-upgrade, or stack-upgrade Conductor executor Job.

          Named Conductor capabilities (non-CAPI path): talos-upgrade, kube-upgrade,
          stack-upgrade. platform-schema.md §5.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UpgradePolicySpec defines the desired state of UpgradePolicy.
            properties:
              clusterRef:
                description: ClusterRef references the TalosCluster this upgrade targets.
                properties:
                  name:
                    description: Name is the object name.
                    type: string
                  namespace:
                    description: Namespace is the object namespace. May be empty for
                      cluster-scoped objects.
                    type: string
                required:
                - name
                type: object
              healthGateConditions:
                description: |-
                  HealthGateConditions is a list of Kubernetes condition types that must be
                  True on each node before the upgrade proceeds to the next node. Used to
                  gate inter-node upgrade sequencing on cluster health.
                items:
                  type: string
                type: array
              lineage:
                description: |-
                  Lineage is the sealed causal chain record for this root declaration.
                  Authored once at object creation time and immutable thereafter.
                  seam-core-schema.md §5, CLAUDE.md §14 Decision 1.
                properties:
                  creatingOperator:
                    description: |-
                      CreatingOperator identifies the Seam Operator that created this object.
                      This is a structured identity carrying the operator name and its deployed
                      version at creation time.
                    properties:
                      name:
                        description: |-
                          Name is the canonical name of the Seam Operator (e.g., platform, guardian,
                          wrapper, conductor).
                        type: string
                      version:
                        description: |-
                          Version is the deployed version of the operator at the time the object was
                          created (e.g., v1.26.5-r3). This allows audit tooling to correlate objects
                          with the operator version that produced them.
                        type: string
                    required:
                    - name
                    - version
                    type: object
                  creationRationale:
                    description: |-
                      CreationRationale is the reason this object was created, drawn from the
                      Seam Core controlled vocabulary defined in rationale.go. It is not a
                      free-text field.
                    enum:
                    - ClusterProvision
                    - ClusterDecommission
                    - SecurityEnforcement
                    - PackExecution
                    - VirtualizationFulfillment
                    - ConductorAssignment
                    - VortexBinding
                    type: string
                  rootGenerationAtCreation:
                    description: |-
                      RootGenerationAtCreation is the metadata.generation of the root declaration
                      at the time this object was created. Together with RootUID, it provides a
                      complete temporal anchor for the derivation record.
                    format: int64
                    type: integer
                  rootKind:
                    description: |-
                      RootKind is the kind of the root declaration that caused this object to
                      exist (e.g., TalosCluster, PackExecution, RBACPolicy).
                    type: string
                  rootName:
                    description: RootName is the name of the root declaration.
                    type: string
                  rootNamespace:
                    description: RootNamespace is the namespace of the root declaration.
                    type: string
                  rootUID:
                    description: |-
                      RootUID is the UID of the root declaration at the time this object was
                      created. Used to verify that no root declaration replacement has occurred.
                    type: string
                required:
                - creatingOperator
                - creationRationale
                - rootGenerationAtCreation
                - rootKind
                - rootName
                - rootNamespace
                - rootUID
                type: object
              rollingStrategy:
                default: sequential
                description: |-
                  RollingStrategy controls the order in which nodes are upgraded.
                  Defaults to sequential.
                enum:
                - sequential
                - parallel
                type: string
              targetKubernetesVersion:
                description: |-
                  TargetKubernetesVersion is the target Kubernetes version for kubernetes and
                  stack upgrades. Required when upgradeType=kubernetes or upgradeType=stack.
                type: string
              targetTalosVersion:
                description: |-
                  TargetTalosVersion is the target Talos version for talos and stack upgrades.
                  Required when upgradeType=talos or upgradeType=stack.
                type: string
              upgradeType:
                allOf:
                - enum:
                  - talos
                  - kubernetes
                  - stack
                - enum:
                  - talos
                  - kubernetes
                  - stack
                description: UpgradeType declares the type of upgrade to perform.
                type: string
            required:
            - clusterRef
            - upgradeType
            type: object
          status:
            description: UpgradePolicyStatus defines the observed state of UpgradePolicy.
            properties:
              conditions:
                description: |-
                  Conditions is the list of status conditions for this UpgradePolicy.
                  Condition types: Ready, Degraded, CAPIDelegated, LineageSynced.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              jobName:
                description: |-
                  JobName is the name of the Conductor executor Job submitted for this upgrade.
                  Only set for the capi.enabled=false (non-CAPI) path.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the spec last
                  reconciled.
                format: int64
                type: integer
              operationResult:
                description: OperationResult is the message from the Conductor OperationResult
                  ConfigMap.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: infrastructurelineageindices.infrastructure.ontai.dev
spec:
  group: infrastructure.ontai.dev
  names:
    kind: InfrastructureLineageIndex
    listKind: InfrastructureLineageIndexList
    plural: infrastructurelineageindices
    shortNames:
    - ili
    singular: infrastructurelineageindex
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.rootBinding.rootKind
      name: RootKind
      type: string
    - jsonPath: .spec.rootBinding.rootName
      name: RootName
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          InfrastructureLineageIndex is the sealed causal chain index for a root
          declaration in the Seam infrastructure domain. It instantiates the abstract
          DomainLineageIndex schema from core.ontai.dev.

          One InfrastructureLineageIndex is created per root declaration by the
          InfrastructureLineageController. All derived objects carry a reference to their
          root's index; they do not carry their own index instances.
          Lineage Index Pattern — seam-core-schema.md §3.
          Controller-authored exclusively — CLAUDE.md §14 Decision 3.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InfrastructureLineageIndexSpec is the spec of an InfrastructureLineageIndex.
            properties:
              descendantRegistry:
                description: |-
                  DescendantRegistry is the list of all objects derived from the root
                  declaration. Appended monotonically as new derived objects are created.
                  Entries are never modified or removed.
                items:
                  description: |-
                    DescendantEntry records a single derived object in the lineage index.
                    Entries are appended monotonically. An entry is never modified or removed
                    except by the retention enforcement loop (which removes stale entries after
                    the retention window elapses).
                  properties:
                    creationRationale:
                      allOf:
                      - enum:
                        - ClusterProvision
                        - ClusterDecommission
                        - SecurityEnforcement
                        - PackExecution
                        - VirtualizationFulfillment
                        - ConductorAssignment
                        - VortexBinding
                      - enum:
                        - ClusterProvision
                        - ClusterDecommission
                        - SecurityEnforcement
                        - PackExecution
                        - VirtualizationFulfillment
                        - ConductorAssignment
                        - VortexBinding
                      description: |-
                        CreationRationale is the reason this derived object was created, drawn from
                        the Seam Core controlled vocabulary (pkg/lineage.CreationRationale).
                      type: string
                    group:
                      description: Group is the API group of the derived object (e.g.,
                        platform.ontai.dev).
                      type: string
                    kind:
                      description: Kind is the kind of the derived object.
                      type: string
                    name:
                      description: Name is the name of the derived object.
                      type: string
                    namespace:
                      description: Namespace is the namespace of the derived object.
                      type: string
                    recordedAt:
                      description: |-
                        RecordedAt is the time this descendant entry was appended to the registry.
                        Used by the retention enforcement loop to determine when a stale entry
                        (referenced object no longer exists) has exceeded its retention window.
                      format: date-time
                      type: string
                    rootGenerationAtCreation:
                      description: |-
                        RootGenerationAtCreation is the metadata.generation of the root declaration
                        at the time this derived object was created.
                      format: int64
                      type: integer
                    seamOperator:
                      description: |-
                        SeamOperator is the name of the Seam Operator that created this derived
                        object (e.g., platform, guardian, wrapper, conductor).
                      type: string
                    uid:
                      description: UID is the UID of the derived object.
                      type: string
                    version:
                      description: Version is the API version of the derived object
                        (e.g., v1alpha1).
                      type: string
                  required:
                  - creationRationale
                  - group
                  - kind
                  - name
                  - namespace
                  - rootGenerationAtCreation
                  - seamOperator
                  - uid
                  - version
                  type: object
                type: array
              domainRef:
                description: |-
                  DomainRef references the DomainLineageIndex at core.ontai.dev that
                  this InfrastructureLineageIndex instantiates. This is the formal
                  traceability link from the infrastructure domain to the domain core.
                  Format: {name}.{group} — e.g. "infrastructure.core.ontai.dev"
                  Set by the InfrastructureLineageController on creation. Validated by the
                  admission webhook: when present, must equal "infrastructure.core.ontai.dev".
                type: string
              policyBindingStatus:
                description: |-
                  PolicyBindingStatus records the InfrastructurePolicy and InfrastructureProfile
                  bound to the root declaration at last evaluation.
                properties:
                  domainPolicyRef:
                    description: |-
                      DomainPolicyRef is the name of the InfrastructurePolicy bound to the root
                      declaration.
                    type: string
                  domainProfileRef:
                    description: |-
                      DomainProfileRef is the name of the InfrastructureProfile bound to the
                      root declaration.
                    type: string
                  driftDetected:
                    description: |-
                      DriftDetected is true if the controller detected drift between the expected
                      state derived from the InfrastructurePolicy and the observed state of
                      derived objects at the last evaluation.
                    type: boolean
                  policyGenerationAtLastEvaluation:
                    description: |-
                      PolicyGenerationAtLastEvaluation is the metadata.generation of the bound
                      InfrastructurePolicy at the time of the last policy evaluation cycle.
                    format: int64
                    type: integer
                type: object
              retentionPolicy:
                description: |-
                  RetentionPolicy declares garbage collection behavior for this index and its
                  stale descendant entries. If absent, controller defaults apply
                  (descendantRetentionDays=30, deleteWithRoot=true).
                properties:
                  deleteWithRoot:
                    default: true
                    description: |-
                      DeleteWithRoot controls whether this InfrastructureLineageIndex is garbage
                      collected when its root declaration is deleted. When true the LineageController
                      adds an ownerReference from the index to the root declaration, causing
                      Kubernetes garbage collection to cascade deletion automatically.

                      Defaults to true.
                    type: boolean
                  descendantRetentionDays:
                    default: 30
                    description: |-
                      DescendantRetentionDays is the number of days a stale descendant entry is
                      retained after its referenced object is confirmed not-found in the API server.
                      After this window elapses the LineageController prunes the entry from the
                      DescendantRegistry.

                      Defaults to 30. Minimum is 1.
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              rootBinding:
                description: |-
                  RootBinding records the root declaration that anchors this lineage index.
                  Immutable after admission. The admission webhook rejects any update that
                  modifies a field in this section.
                properties:
                  rootKind:
                    description: RootKind is the kind of the root declaration (e.g.,
                      TalosCluster, PackExecution).
                    type: string
                  rootName:
                    description: RootName is the name of the root declaration.
                    type: string
                  rootNamespace:
                    description: RootNamespace is the namespace of the root declaration.
                    type: string
                  rootObservedGeneration:
                    description: |-
                      RootObservedGeneration is the metadata.generation of the root declaration
                      when this index was created.
                    format: int64
                    type: integer
                  rootUID:
                    description: RootUID is the UID of the root declaration at time
                      of index creation.
                    type: string
                required:
                - rootKind
                - rootName
                - rootNamespace
                - rootObservedGeneration
                - rootUID
                type: object
            required:
            - rootBinding
            type: object
          status:
            description: |-
              InfrastructureLineageIndexStatus is the observed state of an
              InfrastructureLineageIndex.
            properties:
              conditions:
                description: Conditions holds the standard Kubernetes condition array
                  for this resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the last generation of the InfrastructureLineageIndex
                  that the controller has processed.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: clusterpacks.infra.ontai.dev
spec:
  group: infra.ontai.dev
  names:
    kind: ClusterPack
    listKind: ClusterPackList
    plural: clusterpacks
    shortNames:
    - cp
    singular: clusterpack
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .status.signed
      name: Signed
      type: boolean
    - jsonPath: .status.conditions[?(@.type=="Available")].status
      name: Available
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ClusterPack is the primary pack registration resource for the infra.ontai.dev API group.
          It records an OCI artifact that has been compiled (off-cluster) and is ready for
          runtime delivery. Once registered, ClusterPack spec is immutable. Changes require
          creating a new ClusterPack. CI-INV-002, wrapper-schema.md §3.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ClusterPackSpec defines the desired state of a ClusterPack.
              All fields except lineage are immutable after creation. CI-INV-002.
              wrapper-schema.md §3.
            properties:
              checksum:
                description: |-
                  Checksum is the SHA256 checksum of the pack artifact, used for integrity
                  verification before deployment. Immutable after creation.
                minLength: 1
                type: string
              executionOrder:
                description: |-
                  ExecutionOrder defines the ordered stages in which pack manifests are applied.
                  Stages must be: rbac, storage, stateful, stateless (in that order when present).
                  Immutable after creation.
                items:
                  description: |-
                    ExecutionStage represents a single stage in the pack execution order.
                    wrapper-schema.md §3 ClusterPack spec.executionOrder.
                  properties:
                    manifests:
                      description: Manifests is the list of manifest names to apply
                        in this stage.
                      items:
                        type: string
                      type: array
                    name:
                      description: 'Name is the stage name. Must be one of: rbac,
                        storage, stateful, stateless.'
                      enum:
                      - rbac
                      - storage
                      - stateful
                      - stateless
                      type: string
                  required:
                  - name
                  type: object
                type: array
              lifecyclePolicies:
                description: LifecyclePolicies controls artifact retention behavior.
                properties:
                  retainOnDeletion:
                    default: true
                    description: |-
                      RetainOnDeletion controls whether the OCI artifact is retained when the
                      ClusterPack CR is deleted. Default: true (artifact retained).
                    type: boolean
                type: object
              lineage:
                description: |-
                  Lineage is the sealed causal chain record for this root declaration.
                  Authored once at object creation time and immutable thereafter.
                  The admission webhook rejects any update that modifies this field after creation.
                  seam-core-schema.md §5, CLAUDE.md §14 Decision 1.
                properties:
                  creatingOperator:
                    description: |-
                      CreatingOperator identifies the Seam Operator that created this object.
                      This is a structured identity carrying the operator name and its deployed
                      version at creation time.
                    properties:
                      name:
                        description: |-
                          Name is the canonical name of the Seam Operator (e.g., platform, guardian,
                          wrapper, conductor).
                        type: string
                      version:
                        description: |-
                          Version is the deployed version of the operator at the time the object was
                          created (e.g., v1.26.5-r3). This allows audit tooling to correlate objects
                          with the operator version that produced them.
                        type: string
                    required:
                    - name
                    - version
                    type: object
                  creationRationale:
                    description: |-
                      CreationRationale is the reason this object was created, drawn from the
                      Seam Core controlled vocabulary defined in rationale.go. It is not a
                      free-text field.
                    enum:
                    - ClusterProvision
                    - ClusterDecommission
                    - SecurityEnforcement
                    - PackExecution
                    - VirtualizationFulfillment
                    - ConductorAssignment
                    - VortexBinding
                    type: string
                  rootGenerationAtCreation:
                    description: |-
                      RootGenerationAtCreation is the metadata.generation of the root declaration
                      at the time this object was created. Together with RootUID, it provides a
                      complete temporal anchor for the derivation record.
                    format: int64
                    type: integer
                  rootKind:
                    description: |-
                      RootKind is the kind of the root declaration that caused this object to
                      exist (e.g., TalosCluster, PackExecution, RBACPolicy).
                    type: string
                  rootName:
                    description: RootName is the name of the root declaration.
                    type: string
                  rootNamespace:
                    description: RootNamespace is the namespace of the root declaration.
                    type: string
                  rootUID:
                    description: |-
                      RootUID is the UID of the root declaration at the time this object was
                      created. Used to verify that no root declaration replacement has occurred.
                    type: string
                required:
                - creatingOperator
                - creationRationale
                - rootGenerationAtCreation
                - rootKind
                - rootName
                - rootNamespace
                - rootUID
                type: object
              provenance:
                description: Provenance records build-time metadata for audit and
                  traceability.
                properties:
                  buildID:
                    description: BuildID is the CI/CD build identifier that produced
                      this pack.
                    type: string
                  buildTimestamp:
                    description: BuildTimestamp is when the pack artifact was produced.
                    format: date-time
                    type: string
                  sourceRef:
                    description: SourceRef is the git reference (commit SHA or tag)
                      from which the pack was built.
                    type: string
                type: object
              rbacDigest:
                description: |-
                  RBACDigest is the OCI digest of the RBAC layer of this ClusterPack artifact.
                  Contains ServiceAccount, Role, ClusterRole, RoleBinding, ClusterRoleBinding
                  manifests extracted at compile time. Guardian /rbac-intake processes this layer
                  before workload apply proceeds. Absent on pre-split ClusterPack specs.
                  wrapper-schema.md §4.
                type: string
              registryRef:
                description: RegistryRef identifies the OCI artifact for this pack.
                  Immutable after creation.
                properties:
                  digest:
                    description: Digest is the OCI image digest (e.g., sha256:abc123...).
                      Immutable after creation.
                    type: string
                  url:
                    description: URL is the OCI registry URL including image name
                      (e.g., registry.ontai.dev/packs/my-pack).
                    type: string
                required:
                - digest
                - url
                type: object
              sourceBuildRef:
                description: |-
                  SourceBuildRef is an opaque reference to the build that produced this pack
                  (e.g., a PackBuild CR name or CI job ID). Informational only. Immutable.
                type: string
              targetClusters:
                description: |-
                  TargetClusters is the list of cluster names to which this ClusterPack should
                  be delivered. The ClusterPackReconciler creates one RunnerConfig per entry in
                  seam-tenant-{clusterName} after signing completes. wrapper-schema.md §4.
                items:
                  type: string
                type: array
              version:
                description: |-
                  Version is the semantic version of this pack. Immutable after creation.
                  Must be a valid semver string (e.g., v1.2.3).
                minLength: 1
                type: string
              workloadDigest:
                description: |-
                  WorkloadDigest is the OCI digest of the workload layer of this ClusterPack
                  artifact. Contains all non-RBAC manifests. Applied after guardian RBACProfile
                  for this pack reaches provisioned=true. Absent on pre-split ClusterPack specs.
                  wrapper-schema.md §4.
                type: string
            required:
            - checksum
            - registryRef
            - version
            type: object
          status:
            description: ClusterPackStatus defines the observed state of a ClusterPack.
            properties:
              conditions:
                description: Conditions is the list of status conditions for this
                  ClusterPack.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the generation most recently reconciled.
                format: int64
                type: integer
              packSignature:
                description: |-
                  PackSignature is the base64-encoded Ed25519 signature produced by the
                  management cluster conductor signing loop. Present only when Signed=true.
                type: string
              signed:
                description: |-
                  Signed indicates whether the conductor signing loop has signed this pack.
                  True = conductor has placed its signature annotation and updated this field.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: packexecutions.infra.ontai.dev
spec:
  group: infra.ontai.dev
  names:
    kind: PackExecution
    listKind: PackExecutionList
    plural: packexecutions
    shortNames:
    - pe
    singular: packexecution
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterPackRef.name
      name: Pack
      type: string
    - jsonPath: .spec.targetClusterRef
      name: Cluster
      type: string
    - jsonPath: .status.conditions[?(@.type=="Running")].status
      name: Running
      type: string
    - jsonPath: .status.conditions[?(@.type=="Succeeded")].status
      name: Succeeded
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          PackExecution is the runtime request to apply a ClusterPack to a target cluster.
          The reconciler performs a 4-gate check before submitting a pack-deploy Job via Kueue.
          wrapper-schema.md §3.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              PackExecutionSpec defines the desired state of a PackExecution.
              wrapper-schema.md §3.
            properties:
              admissionProfileRef:
                description: |-
                  AdmissionProfileRef is the name of the RBACProfile that governs this execution.
                  The 4-gate check verifies this profile has reached provisioned=true.
                minLength: 1
                type: string
              clusterPackRef:
                description: ClusterPackRef identifies the ClusterPack to deploy.
                properties:
                  name:
                    description: Name is the name of the ClusterPack CR.
                    minLength: 1
                    type: string
                  version:
                    description: |-
                      Version is the expected version of the ClusterPack. Used to guard against
                      race conditions where the name is reused with a different version.
                    minLength: 1
                    type: string
                required:
                - name
                - version
                type: object
              lineage:
                description: |-
                  Lineage is the sealed causal chain record for this root declaration.
                  Authored once at object creation time and immutable thereafter.
                  seam-core-schema.md §5, CLAUDE.md §14 Decision 1.
                properties:
                  creatingOperator:
                    description: |-
                      CreatingOperator identifies the Seam Operator that created this object.
                      This is a structured identity carrying the operator name and its deployed
                      version at creation time.
                    properties:
                      name:
                        description: |-
                          Name is the canonical name of the Seam Operator (e.g., platform, guardian,
                          wrapper, conductor).
                        type: string
                      version:
                        description: |-
                          Version is the deployed version of the operator at the time the object was
                          created (e.g., v1.26.5-r3). This allows audit tooling to correlate objects
                          with the operator version that produced them.
                        type: string
                    required:
                    - name
                    - version
                    type: object
                  creationRationale:
                    description: |-
                      CreationRationale is the reason this object was created, drawn from the
                      Seam Core controlled vocabulary defined in rationale.go. It is not a
                      free-text field.
                    enum:
                    - ClusterProvision
                    - ClusterDecommission
                    - SecurityEnforcement
                    - PackExecution
                    - VirtualizationFulfillment
                    - ConductorAssignment
                    - VortexBinding
                    type: string
                  rootGenerationAtCreation:
                    description: |-
                      RootGenerationAtCreation is the metadata.generation of the root declaration
                      at the time this object was created. Together with RootUID, it provides a
                      complete temporal anchor for the derivation record.
                    format: int64
                    type: integer
                  rootKind:
                    description: |-
                      RootKind is the kind of the root declaration that caused this object to
                      exist (e.g., TalosCluster, PackExecution, RBACPolicy).
                    type: string
                  rootName:
                    description: RootName is the name of the root declaration.
                    type: string
                  rootNamespace:
                    description: RootNamespace is the namespace of the root declaration.
                    type: string
                  rootUID:
                    description: |-
                      RootUID is the UID of the root declaration at the time this object was
                      created. Used to verify that no root declaration replacement has occurred.
                    type: string
                required:
                - creatingOperator
                - creationRationale
                - rootGenerationAtCreation
                - rootKind
                - rootName
                - rootNamespace
                - rootUID
                type: object
              targetClusterRef:
                description: |-
                  TargetClusterRef is the name of the TalosCluster CR that is the deployment target.
                  The cluster must be Ready before the pack-deploy Job will be admitted.
                minLength: 1
                type: string
            required:
            - admissionProfileRef
            - clusterPackRef
            - targetClusterRef
            type: object
          status:
            description: PackExecutionStatus defines the observed state of a PackExecution.
            properties:
              conditions:
                description: Conditions is the list of status conditions for this
                  PackExecution.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              jobName:
                description: |-
                  JobName is the name of the pack-deploy Kueue Job that was submitted.
                  Present after Job submission.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation most recently reconciled.
                format: int64
                type: integer
              operationResultRef:
                description: |-
                  OperationResultRef is the name of the ConfigMap written by the conductor
                  executor after successful pack-deploy Job completion.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: packinstances.infra.ontai.dev
spec:
  group: infra.ontai.dev
  names:
    kind: PackInstance
    listKind: PackInstanceList
    plural: packinstances
    shortNames:
    - pi
    singular: packinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterPackRef
      name: Pack
      type: string
    - jsonPath: .spec.targetClusterRef
      name: Cluster
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Drifted")].status
      name: Drifted
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          PackInstance records the delivered pack state on a target cluster. It is created
          by wrapper after a successful PackExecution and updated by the drift detection loop.
          One PackInstance per pack per target cluster. wrapper-schema.md §3.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              PackInstanceSpec defines the desired state of a PackInstance.
              wrapper-schema.md §3.
            properties:
              clusterPackRef:
                description: |-
                  ClusterPackRef identifies the ClusterPack that was delivered to produce
                  this instance record.
                minLength: 1
                type: string
              dependencyPolicy:
                description: DependencyPolicy defines behavior when a dependency reports
                  drift.
                properties:
                  onDrift:
                    default: Warn
                    description: |-
                      OnDrift controls how this PackInstance responds when a declared dependency
                      PackInstance reports Drifted=True.
                    enum:
                    - Block
                    - Warn
                    - Ignore
                    type: string
                type: object
              dependsOn:
                description: |-
                  DependsOn is the list of PackInstance names that this instance depends on.
                  The DependencyPolicy.OnDrift setting governs behavior when any listed
                  dependency reports drift.
                items:
                  type: string
                type: array
              lineage:
                description: |-
                  Lineage is the sealed causal chain record for this root declaration.
                  Authored once at object creation time and immutable thereafter.
                  seam-core-schema.md §5, CLAUDE.md §14 Decision 1.
                properties:
                  creatingOperator:
                    description: |-
                      CreatingOperator identifies the Seam Operator that created this object.
                      This is a structured identity carrying the operator name and its deployed
                      version at creation time.
                    properties:
                      name:
                        description: |-
                          Name is the canonical name of the Seam Operator (e.g., platform, guardian,
                          wrapper, conductor).
                        type: string
                      version:
                        description: |-
                          Version is the deployed version of the operator at the time the object was
                          created (e.g., v1.26.5-r3). This allows audit tooling to correlate objects
                          with the operator version that produced them.
                        type: string
                    required:
                    - name
                    - version
                    type: object
                  creationRationale:
                    description: |-
                      CreationRationale is the reason this object was created, drawn from the
                      Seam Core controlled vocabulary defined in rationale.go. It is not a
                      free-text field.
                    enum:
                    - ClusterProvision
                    - ClusterDecommission
                    - SecurityEnforcement
                    - PackExecution
                    - VirtualizationFulfillment
                    - ConductorAssignment
                    - VortexBinding
                    type: string
                  rootGenerationAtCreation:
                    description: |-
                      RootGenerationAtCreation is the metadata.generation of the root declaration
                      at the time this object was created. Together with RootUID, it provides a
                      complete temporal anchor for the derivation record.
                    format: int64
                    type: integer
                  rootKind:
                    description: |-
                      RootKind is the kind of the root declaration that caused this object to
                      exist (e.g., TalosCluster, PackExecution, RBACPolicy).
                    type: string
                  rootName:
                    description: RootName is the name of the root declaration.
                    type: string
                  rootNamespace:
                    description: RootNamespace is the namespace of the root declaration.
                    type: string
                  rootUID:
                    description: |-
                      RootUID is the UID of the root declaration at the time this object was
                      created. Used to verify that no root declaration replacement has occurred.
                    type: string
                required:
                - creatingOperator
                - creationRationale
                - rootGenerationAtCreation
                - rootKind
                - rootName
                - rootNamespace
                - rootUID
                type: object
              targetClusterRef:
                description: |-
                  TargetClusterRef is the name of the TalosCluster CR to which this pack
                  has been delivered.
                minLength: 1
                type: string
              version:
                description: |-
                  Version is the semantic version of the ClusterPack that was delivered.
                  Set from ClusterPack.spec.version at PackInstance creation time.
                  Used by DSNSReconciler to emit pack.{name}.{version}.wrapper.{cluster} DNS records.
                type: string
            required:
            - clusterPackRef
            - targetClusterRef
            type: object
          status:
            description: PackInstanceStatus defines the observed state of a PackInstance.
            properties:
              conditions:
                description: Conditions is the list of status conditions for this
                  PackInstance.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              deliveredAt:
                description: |-
                  DeliveredAt records when the pack was most recently confirmed delivered
                  (PackReceipt observed in a Ready state).
                format: date-time
                type: string
              driftSummary:
                description: |-
                  DriftSummary is a human-readable one-line summary of the current drift
                  state as reported by the conductor drift detection loop via PackReceipt.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation most recently reconciled.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: infrastructurelineageindices.infrastructure.ontai.dev
spec:
  group: infrastructure.ontai.dev
  names:
    kind: InfrastructureLineageIndex
    listKind: InfrastructureLineageIndexList
    plural: infrastructurelineageindices
    shortNames:
    - ili
    singular: infrastructurelineageindex
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.rootBinding.rootKind
      name: RootKind
      type: string
    - jsonPath: .spec.rootBinding.rootName
      name: RootName
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          InfrastructureLineageIndex is the sealed causal chain index for a root
          declaration in the Seam infrastructure domain. It instantiates the abstract
          DomainLineageIndex schema from core.ontai.dev.

          One InfrastructureLineageIndex is created per root declaration by the
          InfrastructureLineageController. All derived objects carry a reference to their
          root's index; they do not carry their own index instances.
          Lineage Index Pattern — seam-core-schema.md §3.
          Controller-authored exclusively — CLAUDE.md §14 Decision 3.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InfrastructureLineageIndexSpec is the spec of an InfrastructureLineageIndex.
            properties:
              descendantRegistry:
                description: |-
                  DescendantRegistry is the list of all objects derived from the root
                  declaration. Appended monotonically as new derived objects are created.
                  Entries are never modified or removed.
                items:
                  description: |-
                    DescendantEntry records a single derived object in the lineage index.
                    Entries are appended monotonically. An entry is never modified or removed
                    except by the retention enforcement loop (which removes stale entries after
                    the retention window elapses).
                  properties:
                    creationRationale:
                      allOf:
                      - enum:
                        - ClusterProvision
                        - ClusterDecommission
                        - SecurityEnforcement
                        - PackExecution
                        - VirtualizationFulfillment
                        - ConductorAssignment
                        - VortexBinding
                      - enum:
                        - ClusterProvision
                        - ClusterDecommission
                        - SecurityEnforcement
                        - PackExecution
                        - VirtualizationFulfillment
                        - ConductorAssignment
                        - VortexBinding
                      description: |-
                        CreationRationale is the reason this derived object was created, drawn from
                        the Seam Core controlled vocabulary (pkg/lineage.CreationRationale).
                      type: string
                    group:
                      description: Group is the API group of the derived object (e.g.,
                        platform.ontai.dev).
                      type: string
                    kind:
                      description: Kind is the kind of the derived object.
                      type: string
                    name:
                      description: Name is the name of the derived object.
                      type: string
                    namespace:
                      description: Namespace is the namespace of the derived object.
                      type: string
                    recordedAt:
                      description: |-
                        RecordedAt is the time this descendant entry was appended to the registry.
                        Used by the retention enforcement loop to determine when a stale entry
                        (referenced object no longer exists) has exceeded its retention window.
                      format: date-time
                      type: string
                    rootGenerationAtCreation:
                      description: |-
                        RootGenerationAtCreation is the metadata.generation of the root declaration
                        at the time this derived object was created.
                      format: int64
                      type: integer
                    seamOperator:
                      description: |-
                        SeamOperator is the name of the Seam Operator that created this derived
                        object (e.g., platform, guardian, wrapper, conductor).
                      type: string
                    uid:
                      description: UID is the UID of the derived object.
                      type: string
                    version:
                      description: Version is the API version of the derived object
                        (e.g., v1alpha1).
                      type: string
                  required:
                  - creationRationale
                  - group
                  - kind
                  - name
                  - namespace
                  - rootGenerationAtCreation
                  - seamOperator
                  - uid
                  - version
                  type: object
                type: array
              domainRef:
                description: |-
                  DomainRef references the DomainLineageIndex at core.ontai.dev that
                  this InfrastructureLineageIndex instantiates. This is the formal
                  traceability link from the infrastructure domain to the domain core.
                  Format: {name}.{group} — e.g. "infrastructure.core.ontai.dev"
                  Set by the InfrastructureLineageController on creation. Validated by the
                  admission webhook: when present, must equal "infrastructure.core.ontai.dev".
                type: string
              policyBindingStatus:
                description: |-
                  PolicyBindingStatus records the InfrastructurePolicy and InfrastructureProfile
                  bound to the root declaration at last evaluation.
                properties:
                  domainPolicyRef:
                    description: |-
                      DomainPolicyRef is the name of the InfrastructurePolicy bound to the root
                      declaration.
                    type: string
                  domainProfileRef:
                    description: |-
                      DomainProfileRef is the name of the InfrastructureProfile bound to the
                      root declaration.
                    type: string
                  driftDetected:
                    description: |-
                      DriftDetected is true if the controller detected drift between the expected
                      state derived from the InfrastructurePolicy and the observed state of
                      derived objects at the last evaluation.
                    type: boolean
                  policyGenerationAtLastEvaluation:
                    description: |-
                      PolicyGenerationAtLastEvaluation is the metadata.generation of the bound
                      InfrastructurePolicy at the time of the last policy evaluation cycle.
                    format: int64
                    type: integer
                type: object
              retentionPolicy:
                description: |-
                  RetentionPolicy declares garbage collection behavior for this index and its
                  stale descendant entries. If absent, controller defaults apply
                  (descendantRetentionDays=30, deleteWithRoot=true).
                properties:
                  deleteWithRoot:
                    default: true
                    description: |-
                      DeleteWithRoot controls whether this InfrastructureLineageIndex is garbage
                      collected when its root declaration is deleted. When true the LineageController
                      adds an ownerReference from the index to the root declaration, causing
                      Kubernetes garbage collection to cascade deletion automatically.

                      Defaults to true.
                    type: boolean
                  descendantRetentionDays:
                    default: 30
                    description: |-
                      DescendantRetentionDays is the number of days a stale descendant entry is
                      retained after its referenced object is confirmed not-found in the API server.
                      After this window elapses the LineageController prunes the entry from the
                      DescendantRegistry.

                      Defaults to 30. Minimum is 1.
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              rootBinding:
                description: |-
                  RootBinding records the root declaration that anchors this lineage index.
                  Immutable after admission. The admission webhook rejects any update that
                  modifies a field in this section.
                properties:
                  rootKind:
                    description: RootKind is the kind of the root declaration (e.g.,
                      TalosCluster, PackExecution).
                    type: string
                  rootName:
                    description: RootName is the name of the root declaration.
                    type: string
                  rootNamespace:
                    description: RootNamespace is the namespace of the root declaration.
                    type: string
                  rootObservedGeneration:
                    description: |-
                      RootObservedGeneration is the metadata.generation of the root declaration
                      when this index was created.
                    format: int64
                    type: integer
                  rootUID:
                    description: RootUID is the UID of the root declaration at time
                      of index creation.
                    type: string
                required:
                - rootKind
                - rootName
                - rootNamespace
                - rootObservedGeneration
                - rootUID
                type: object
            required:
            - rootBinding
            type: object
          status:
            description: |-
              InfrastructureLineageIndexStatus is the observed state of an
              InfrastructureLineageIndex.
            properties:
              conditions:
                description: Conditions holds the standard Kubernetes condition array
                  for this resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the last generation of the InfrastructureLineageIndex
                  that the controller has processed.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: seammemberships.infrastructure.ontai.dev
spec:
  group: infrastructure.ontai.dev
  names:
    kind: SeamMembership
    listKind: SeamMembershipList
    plural: seammemberships
    shortNames:
    - sm
    singular: seammembership
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.admitted
      name: Admitted
      type: boolean
    - jsonPath: .spec.tier
      name: Tier
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SeamMembership is the formal join declaration for an operator wishing to
          become a member of the Seam infrastructure family. Guardian validates and
          admits the membership after verifying the operator's RBACProfile. Operators
          that are not members may not be allocated PermissionSnapshots.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SeamMembershipSpec defines the desired state of a SeamMembership.
            properties:
              appIdentityRef:
                description: |-
                  AppIdentityRef references the operator's application-layer identity.
                  For Seam family operators this is the operator name (guardian, platform,
                  wrapper, conductor, seam-core, vortex).
                  Format: {name} — references an AppIdentity in the domain-core (future)
                  or the operator's service account name (current).
                type: string
              domainIdentityRef:
                description: |-
                  DomainIdentityRef references the DomainIdentity at core.ontai.dev
                  that this operator traces to. Must match the domainIdentityRef on
                  the operator's RBACProfile.
                type: string
              principalRef:
                description: |-
                  PrincipalRef is the Kubernetes service account that this operator
                  runs as. Format: system:serviceaccount:{namespace}:{name}
                  Must match the principalRef on the operator's RBACProfile.
                type: string
              tier:
                description: |-
                  Tier declares the membership tier. Valid values: infrastructure, application.
                  infrastructure: Seam family operators (guardian, platform, wrapper, etc.)
                  application: Application operators (vortex, future app operators)
                enum:
                - infrastructure
                - application
                type: string
            required:
            - appIdentityRef
            - domainIdentityRef
            - principalRef
            - tier
            type: object
          status:
            description: SeamMembershipStatus defines the observed state of a SeamMembership.
            properties:
              admitted:
                description: Admitted is true when Guardian has validated and admitted
                  this member.
                type: boolean
              admittedAt:
                description: AdmittedAt is the timestamp when Guardian admitted this
                  member.
                format: date-time
                type: string
              conditions:
                description: Conditions is the list of status conditions for this
                  SeamMembership.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              permissionSnapshotRef:
                description: |-
                  PermissionSnapshotRef is the name of the PermissionSnapshot Guardian
                  resolved for this member. Set after admission.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
