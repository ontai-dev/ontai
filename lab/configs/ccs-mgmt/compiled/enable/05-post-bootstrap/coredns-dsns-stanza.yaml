# CoreDNS DSNS Stanza Reference — DO NOT apply directly.
# The Corefile.seam-addendum key contains the stanza block to append
# to the active Corefile in the coredns ConfigMap (kube-system).
# Operator procedure:
#   kubectl edit configmap coredns -n kube-system
#   Append the Corefile.seam-addendum value to the Corefile key.
# Generated by: compiler enable
# seam-core-schema.md §8 Decision 3.
---
apiVersion: v1
data:
  Corefile.seam-addendum: |
    seam.ontave.dev. {
        file /etc/coredns/dsns/zone.db
        reload 10s
    }
kind: ConfigMap
metadata:
  annotations:
    governance.infrastructure.ontai.dev/owner: seam-core
  name: coredns
  namespace: kube-system
