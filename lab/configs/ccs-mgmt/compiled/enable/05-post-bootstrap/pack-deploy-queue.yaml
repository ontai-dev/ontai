# Kueue LocalQueue — pack-deploy-queue in seam-tenant-ccs-mgmt
# References ClusterQueue seam-pack-deploy (Kueue admin must provision the ClusterQueue).
# Required for pack-deploy Job admission in the tenant namespace.
# Generated by: compiler enable (phase 05 post-bootstrap)
# wrapper-schema.md §9, conductor-schema.md §5.
---
apiVersion: kueue.x-k8s.io/v1beta1
kind: LocalQueue
metadata:
  labels:
    ontai.dev/managed-by: compiler
    platform.ontai.dev/cluster: ccs-mgmt
  name: pack-deploy-queue
  namespace: seam-tenant-ccs-mgmt
spec:
  clusterQueue: seam-pack-deploy
