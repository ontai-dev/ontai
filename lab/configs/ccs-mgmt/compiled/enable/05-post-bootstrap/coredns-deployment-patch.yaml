# CoreDNS Deployment Strategic Merge Patch
# Apply with: kubectl patch deployment coredns -n kube-system --patch-file coredns-deployment-patch.yaml
# Adds dsns-zone ConfigMap as volume and mounts at /etc/coredns/dsns/.
# CoreDNS file plugin reads /etc/coredns/dsns/zone.db per the stanza in coredns-dsns-stanza.yaml.
# Generated by: compiler enable
# seam-core-schema.md §8 Decision 3.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: coredns
  namespace: kube-system
spec:
  template:
    spec:
      containers:
      - name: coredns
        volumeMounts:
        - mountPath: /etc/coredns/dsns
          name: dsns-zone
          readOnly: true
      volumes:
      - configMap:
          name: dsns-zone
        name: dsns-zone
