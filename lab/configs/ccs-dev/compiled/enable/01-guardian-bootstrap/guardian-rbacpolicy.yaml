# Bootstrap RBACPolicy -- management-policy
# Generated by: compiler enable (phase 1 guardian-bootstrap)
# Layer 1 fleet-wide governing policy (guardian-schema.md §19 Layer 1).
# Governs all seam operator RBACProfiles (guardian, platform, wrapper, conductor, seam-core).
# enforcementMode=audit: Guardian records violations but does not block during bootstrap.
# guardian-schema.md §6, §19.
---
apiVersion: security.ontai.dev/v1alpha1
kind: RBACPolicy
metadata:
  labels:
    ontai.dev/managed-by: compiler
    ontai.dev/policy-type: management
  name: management-policy
  namespace: seam-system
spec:
  enforcementMode: audit
  maximumPermissionSetRef: management-maximum
  subjectScope: platform
