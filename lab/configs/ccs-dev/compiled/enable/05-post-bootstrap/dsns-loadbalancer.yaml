# DSNS LoadBalancer Service — exposes CoreDNS on port 53 UDP+TCP at the DSNS IP.
# Namespace: kube-system (required — Service selector must be in same namespace as pods).
# IP allocated via lbipam.cilium.io/ips annotation (Cilium IPAM). Provide --dsns-ip.
# Generated by: compiler enable
# seam-core-schema.md §8 Decision 3.
---
apiVersion: v1
kind: Service
metadata:
  annotations:
    governance.infrastructure.ontai.dev/owner: seam-core
    lbipam.cilium.io/ips: 10.20.0.225
  name: dsns-loadbalancer
  namespace: kube-system
spec:
  ports:
  - name: dns-udp
    port: 53
    protocol: UDP
    targetPort: 53
  - name: dns-tcp
    port: 53
    protocol: TCP
    targetPort: 53
  selector:
    k8s-app: kube-dns
  type: LoadBalancer
status:
  loadBalancer: {}
