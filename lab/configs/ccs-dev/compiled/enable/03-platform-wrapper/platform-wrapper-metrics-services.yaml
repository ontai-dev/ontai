# Prometheus Metrics Services
# Generated by: compiler enable
# Port 8080 named 'metrics' for each operator. Controlled by METRICS_ADDR.
# ServiceMonitor CRDs for Prometheus Operator scrape configuration are
# deferred to a post-e2e observability session.
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: metrics
    app.kubernetes.io/name: platform
  name: platform-metrics
  namespace: seam-system
spec:
  ports:
  - name: metrics
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app.kubernetes.io/name: platform
status:
  loadBalancer: {}
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: metrics
    app.kubernetes.io/name: wrapper
  name: wrapper-metrics
  namespace: seam-system
spec:
  ports:
  - name: metrics
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app.kubernetes.io/name: wrapper
status:
  loadBalancer: {}
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: metrics
    app.kubernetes.io/name: seam-core
  name: seam-core-metrics
  namespace: seam-system
spec:
  ports:
  - name: metrics
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app.kubernetes.io/name: seam-core
status:
  loadBalancer: {}
