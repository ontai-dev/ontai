# Guardian Lineage Immutability ValidatingWebhookConfiguration
# Generated by: compiler enable (phase 2 guardian-deploy)
# Enforces spec.lineage immutability on all security.ontai.dev root declarations.
# Rejects any UPDATE that attempts to alter a lineage field after creation.
# caBundle injected by cert-manager CA injector via cert-manager.io/inject-ca-from annotation.
# CLAUDE.md §14 Decision 1. guardian-schema.md §5.
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  annotations:
    cert-manager.io/inject-ca-from: seam-system/guardian-webhook-cert
    ontai.dev/managed-by: compiler
  name: guardian-lineage-immutability-webhook
webhooks:
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: guardian
      namespace: seam-system
      path: /validate-lineage
      port: 443
  failurePolicy: Fail
  name: validate-lineage.security.ontai.dev
  namespaceSelector:
    matchExpressions:
    - key: seam.ontai.dev/webhook-mode
      operator: NotIn
      values:
      - exempt
  rules:
  - apiGroups:
    - security.ontai.dev
    apiVersions:
    - v1alpha1
    operations:
    - UPDATE
    resources:
    - rbacpolicies
    - rbacprofiles
    - identitybindings
    - identityproviders
    - permissionsets
    scope: '*'
  sideEffects: None
