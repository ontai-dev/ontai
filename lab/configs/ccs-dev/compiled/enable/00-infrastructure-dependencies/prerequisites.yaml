# Platform prerequisites declaration — seam-platform-prerequisites ConfigMap
# Generated by: compiler enable (phase 0 infrastructure-dependencies)
# The operator must provision all listed prerequisites before applying
# phase 01-guardian-bootstrap. The compiler does not create these resources.
# conductor-schema.md §9.
---
apiVersion: v1
data:
  certificate-manager: cert-manager v1.13 or later if webhook TLS is managed externally.
    Optional if self-signed certificates are used.
  database: CNPG v1.25 or later. CRD clusters.postgresql.cnpg.io must exist. One Cluster
    named guardian-db in seam-system must be Ready. A Secret named guardian-db-credentials
    must exist in seam-system with keys username and password.
  job-scheduler: Kueue v0.16.2 or later. ClusterQueue and LocalQueue CRDs must exist
    and be registered. Kueue mutating webhook is scoped to ont-managed namespaces
    immediately after kueue-controller.yaml is applied in Phase 00 by enable-ccs-mgmt.sh.
    C-KUEUE-WEBHOOK.
  storage: A default StorageClass must exist for CNPG PVCs.
kind: ConfigMap
metadata:
  annotations:
    seam.ontai.dev/apply-before: 01-guardian-bootstrap
    seam.ontai.dev/description: Prerequisites that must be satisfied by the operator
      before applying phase 01-guardian-bootstrap.
  labels:
    seam.ontai.dev/phase: prerequisites
  name: seam-platform-prerequisites
  namespace: seam-system
