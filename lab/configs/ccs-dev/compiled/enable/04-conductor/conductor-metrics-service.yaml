# Prometheus metrics Service — conductor in ont-system
# Generated by: compiler enable
# Port 8080 named 'metrics'. Controlled by METRICS_ADDR in the Deployment.
# ServiceMonitor CRDs for Prometheus Operator scrape configuration are
# deferred to a post-e2e observability session.
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: metrics
    app.kubernetes.io/name: conductor
  name: conductor-metrics
  namespace: ont-system
spec:
  ports:
  - name: metrics
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app.kubernetes.io/name: conductor
status:
  loadBalancer: {}
