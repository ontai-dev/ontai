# Conductor Deployment (CONDUCTOR_ROLE=tenant)
# Generated by: compiler enable (phase 4 conductor)
# conductor-schema.md §15 Role Declaration Contract.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: operator
    app.kubernetes.io/name: conductor
    ontai.dev/managed-by: compiler
  name: conductor
  namespace: ont-system
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: conductor
  strategy: {}
  template:
    metadata:
      annotations:
        platform.ontai.dev/cluster-ref: ccs-dev
        platform.ontai.dev/role: tenant
      labels:
        app.kubernetes.io/component: operator
        app.kubernetes.io/name: conductor
    spec:
      containers:
      - args:
        - agent
        env:
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: CONDUCTOR_ROLE
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['platform.ontai.dev/role']
        - name: CLUSTER_REF
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['platform.ontai.dev/cluster-ref']
        - name: SIGNING_PUBLIC_KEY_PATH
          value: /etc/conductor/signing/public.pem
        image: 10.20.0.1:5000/ontai-dev/conductor:dev
        imagePullPolicy: Always
        name: conductor
        resources: {}
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          seccompProfile:
            type: RuntimeDefault
        volumeMounts:
        - mountPath: /etc/conductor/signing
          name: conductor-signing-key
          readOnly: true
      securityContext:
        runAsNonRoot: true
      serviceAccountName: conductor
      terminationGracePeriodSeconds: 30
      volumes:
      - name: conductor-signing-key
        secret:
          secretName: conductor-signing-key
status: {}
