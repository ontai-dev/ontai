# Seam Platform — Fast Bootstrap Context
> Read this file first. PROGRESS.md is the detailed audit record — consult on demand.

---

## 0. Anti-Patterns (FORBIDDEN)
- Adding Co-Authored-By trailers to commits
- Pushing the ontai root repository to a git remote — ontai root is intentionally not a git remote repository; do not attempt to push it
- Creating or assuming a namespace outside the canonical model (seam-system, ont-system, seam-tenant-{cluster-name}) without a Governor directive

## 1. Platform State

| Component    | Last Commit | Status                        | Next Pending Work                                           |
|--------------|-------------|-------------------------------|-------------------------------------------------------------|
| conductor    | 4fbbffd     | WS1: SigningLoop signs PackInstance+PermissionSnapshot CRs with Ed25519 (SIGNING_PRIVATE_KEY_PATH gate, management cluster only, INV-026); WS2: local PermissionService gRPC (SnapshotStore + LocalService + hand-written service descriptor, PERMISSION_SERVICE_ADDR, all clusters); WS3: SnapshotPullLoop — target cluster pulls PermissionSnapshot from management, verifies Ed25519 (SIGNING_PUBLIC_KEY_PATH + MGMT_KUBECONFIG_PATH gates), calls SnapshotStore.Update; DegradedSecurityState on failure; bootstrap window mode (INV-020); session/25: CapabilityRBACProvision — Ed25519 signature verification (INV-026), bootstrap window bypass (INV-020), SigningPublicKey field added to ExecuteClients; session/governor (WS1-Compiler): Compiler subcommand model corrected — CR compiler (bootstrap/launch/enable/packbuild/domain) — F-P2 closed; session/governor (F-P4): WS1 three-image Dockerfile scaffolding (Dockerfile.compiler/execute/agent + docker-build Makefile target); WS2 RunnerConfig self-operation fields (MaintenanceTargetNodes/OperatorLeaderNode/SelfOperation + JobSpec.NodeExclusions + ResolveNodeExclusionsFromRunnerConfig + 5 new tests); WS3 compiler real implementations (platform+wrapper+seam-core local replace directives, compileBootstrap/compileLaunch/compileEnable produce TalosCluster CR YAML, compilePackBuild produces ClusterPack CR YAML); 7 suites green (F-P4 closed); session/29 WS3: compiler maintenance subcommand — resolves kubeconfig (flag→KUBECONFIG env→~/.kube/config), reads platform-leader Lease → pod → NodeName, validates S3 Secret for etcd-backup ops, validates target nodes, produces MaintenanceBundle CR YAML; 7 suites green; session/31 (F-P6): WS1: internal/catalog/ embedded FS with Go embed (entries/ YAML templates for Cilium/CNPG/Kueue/cert-manager/local-path-provisioner), CatalogEntry/RenderParams/ComponentDescriptor types, catalog.All()/Lookup()/AvailableNames()/Render()/DescriptorScaffold(); 14 unit tests in test/unit/catalog/; 8 suites green — 5eaceed; WS2: compiler component subcommand wired into main.go (catalog mode, descriptor mode, discover mode), compile_component.go + compile_component_test.go; 15 new tests; 9 suites green — 6000606. F-P6 closed. session/32: Compiler bootstrap/launch/enable real implementations. WS1 (d6bac86): compileBootstrap uses talos machinery (generate.NewInput, secrets.NewBundle, node-per-Secret, seam-mc-{cluster}-{hostname} naming, TalosCluster CR YAML, bootstrap-sequence.yaml, BootstrapSequence with json+yaml tags); 10 tests. WS2 (51264ca): compileLaunchBundle uses go embed from all 5 CRD repos (platform, guardian, wrapper, seam-core, conductor) — guardian added to go.mod replace block; crds.yaml bundle sorted+deterministic; 8 tests. WS3 (3745987): compileEnableBundle produces crds.yaml+operators.yaml+rbac.yaml+leaderelection.yaml+rbacprofiles.yaml; Conductor carries CONDUCTOR_ROLE=management (§15); all 5 operators with correct namespaces (conductor→ont-system, others→seam-system); RBACProfile CRs with review-required annotation; 11 tests. 9 suites green. session/governor (RunnerConfig Execution Model §17): WS1 (b62be77): RunnerConfigStep, StepPhase, ConfigMapRef, RunnerConfigStepResult added to runnerlib; Steps to RunnerConfigSpec, StepResults to RunnerConfigStatus; CRD YAML updated. WS2 (68d11f3): RunExecute replaced with step sequencer — StepExecutor + StepStatusWriter interfaces, NoopStepStatusWriter, capabilityStepExecutor in main.go; 9 kernel tests (single-step, multi-step, halt-on-failure, partial completion, error propagation). 9 suites green. session/35 (Gap 29, 38afdda): compileEnableBundle restructured to 5 phased subdirectories (01-guardian-bootstrap, 02-guardian-deploy, 03-platform-wrapper, 04-conductor, 05-post-bootstrap); each phase carries phase-meta.yaml (name, order, readinessGate, applyOrder); namespace-labels.yaml in phase 1 stamps seam.ontai.dev/webhook-mode=exempt on kube-system+seam-system as SSA metadata-only patches (satisfies guardian 25c9e93 WS3 CheckBootstrapLabels contract); CRDs filtered by API group from compileLaunchBundle; 15 enable tests all pass; 9 suites green. Gap 29 resolved. session/38 WS1 (d23a6fc): SigningLoop extended — after signing PackInstance CR, stores signed artifact as Secret seam-pack-signed-{clusterName}-{packInstanceName} in seam-tenant-{clusterName}; Secret data: artifact (base64 JSON) + signature (base64 Ed25519); idempotent (skip if sig matches, overwrite if differs); 3 new tests; 9 suites green. session/38 WS2 (f7a127e): PackInstancePullLoop — tenant cluster polls management cluster Secrets seam-pack-signed-{clusterName}-*, verifies Ed25519 signature (INV-026), creates/updates PackReceipt (Verified=True/False); bootstrap window mode (INV-020); idempotent; wired into agent.go alongside SnapshotPullLoop with shared mgmtDynamicClient; 9 unit tests; all 9 suites green. Gap 28 resolved. session/40 WS2 (19dcc03): e2e suite — Ginkgo suite bootstrap, MGMT_KUBECONFIG/TENANT_KUBECONFIG/REGISTRY_ADDR/MGMT_CLUSTER_NAME/TENANT_CLUSTER_NAME env vars, skip-all when MGMT_KUBECONFIG absent, make e2e target. session/40 WS4 (4fbbffd): 3 e2e stub files (signing_loop_test.go, packinstance_pull_loop_test.go, snapshot_pull_loop_test.go — 15 It blocks all skipping; each with pre-condition comment block); all suites green. | — |
| guardian     | 34437c0     | WS1: SealedCausalChain spec.lineage added to RBACPolicy, RBACProfile, IdentityBinding, IdentityProvider, PermissionSet; CRDs regenerated; seam-core dependency wired; WS2: LineageSynced=False/LineageControllerAbsent initialization in all 5 reconcilers; lineage_conditions.go added; WS3 (session/24): lineage_immutability.go + lineage_handler.go + RegisterLineage — rejects spec.lineage UPDATE on all 5 root-declaration kinds; 16 new unit tests green; session/34 WS1: namespace tiered admission mode (NamespaceModeResolver, KubeNamespaceModeResolver, StaticNamespaceModeResolver, seam.ontai.dev/webhook-mode label, exempt/observe/enforce tiers, ObservedDeny logging, 13 new unit tests); WS2: Guardian singleton CRD, WebhookModeGate, NamespaceEnforcementRegistry, GuardedNamespaceModeResolver, BootstrapController (Initialising→ObserveOnly ratchet, per-namespace enforcement promotion, 12 new unit tests); WS3: CheckBootstrapLabels startup gate — seam-system must carry webhook-mode=exempt before webhook registration, ErrBootstrapLabelAbsent, 5 new unit tests; 4 suites green; session/37 WS2 (8155ba2): lineage_conditions.go — local ConditionTypeLineageSynced/ReasonLineageControllerAbsent replaced with re-exports from seam-core/pkg/conditions; all 5 reconcilers compile unchanged via re-export pattern; SC-INV-002 closed for guardian. session/40 WS2 (ec9c701): e2e suite — Ginkgo suite bootstrap, env vars, skip-all when MGMT_KUBECONFIG absent, make e2e target. session/40 WS4 (34437c0): 3 e2e stub files (webhook_test.go, rbacprofile_test.go, identityprovider_test.go — 10 It blocks all skipping; each with pre-condition comment block); all suites green. | — |
| platform     | 2264170     | TalosCluster + SeamInfrastructureCluster/Machine CRD types, all three reconcilers (TalosCluster, SIC, SIM), SIC/SIM CRDs with lineage + controller-gen clean, SIM implements 6-step machineconfig delivery via TalosMachineConfigApplier (only file with talos goclient, CP-INV-001 clean), unit tests green, go build clean; session/governor: all 8 day-2 CRD types (EtcdMaintenance, NodeMaintenance, PKIRotation, ClusterReset, HardeningProfile, UpgradePolicy, NodeOperation, ClusterMaintenance) with spec.lineage; all 8 day-2 reconcilers implemented with CapabilityUnavailable/PendingApproval gates; wired into main.go; 12 new unit tests green (F-P1 closed, F-P3 closed); session/29 WS1: resolveOperatorLeaderNode (platform-leader Lease → pod → NodeName) + jobSpecWithExclusions (NotIn NodeAffinity) wired into all 6 Job-submitting day-2 reconcilers; EtcdMaintenance S3 resolution hierarchy (EtcdBackupS3SecretRef spec field + seam-etcd-backup-config default + EtcdBackupDestinationAbsent condition); 6 new unit tests + 1 updated; all 17 tests green; session/29 WS2: MaintenanceBundle CRD (operation enum drain/upgrade/etcd-backup/machineconfig-rotation, maintenanceTargetNodes, operatorLeaderNode, s3ConfigSecretRef, spec.lineage); stub reconciler (F-P5); CRD YAML generated; wired into main.go; session/30 WS1: MaintenanceBundleReconciler full implementation — reads pre-encoded scheduling context, submits Job via jobSpecWithExclusions, transitions Pending→Ready/Degraded; JobName+OperationResult status fields; CRD YAML regenerated; 5 new tests; session/30 WS2: EnsureConductorDeploymentOnTargetCluster in TalosClusterReconciler — creates Conductor agent Deployment in ont-system on target cluster before marking Ready; CAPI kubeconfig Secret lookup; CONDUCTOR_ROLE=tenant env stamp; BuildConductorAgentDeployment; 3 new tests; all tests green; session/33 WS1 (8db456d): migrate day-2 reconcilers from batchv1.Job to RunnerConfig §17 multi-step model — OperationalRunnerConfig typed struct (runnerconfig_cr.go), 4-step NodeMaintenance RC (cordon→drain→operate→uncordon), 2-step UpgradeTypeStack RC (talos-upgrade→kube-upgrade DependsOn), S3 params in EtcdMaintenance backup step.Parameters, ownerReference on all RCs, all tests migrated to assert RunnerConfig list, new stack-upgrade and 4-step tests added, all tests green (F-P7 WS1 closed); session/33 WS2 (24ac26b): port-50000 retry/backoff — Port50000Backoff (10s base, 5min cap, 2^n formula), SeamInfrastructureMachineReconciler increments ApplyAttempts on failure and returns RequeueAfter=backoff with nil error, TalosClusterReconciler.checkMachineReachability lists SIMs after CAPI object creation — CP machine with ApplyAttempts>=3 sets ControlPlaneUnreachable=True and halts, worker sets PartialWorkerAvailability=True and continues, 5 new tests all green (F-P7 WS2 closed); session/36 WS1 (82533c8): ConductorReady condition — EnsureConductorDeploymentOnTargetCluster now returns (bool, error), checks Deployment Available condition after ensure; ensureConductorReadyAndTransition replaces both old call sites; ConditionTypeConductorReady/ReasonConductorDeploymentAvailable/Unavailable added to types; RemoteConductorAvailableFn injectable override for tests; 3 new unit tests (Available→Ready, Unavailable→requeue, condition transition); Gap 27 WS1 resolved; session/39 (871db38): Gap 31 — lineage_conditions.go in both platform/api/v1alpha1 and platform/api/infrastructure/v1alpha1 replaced with re-export pattern from seam-core/pkg/conditions; SC-INV-002 closed for platform. session/40 WS2 (60b0095): e2e suite — Ginkgo suite bootstrap, env vars, skip-all when MGMT_KUBECONFIG absent, make e2e target. session/40 WS4 (2264170): 3 e2e stub files (taloscluster_bootstrap_test.go, conductorready_test.go, port50000_test.go — 12 It blocks all skipping; each with pre-condition comment block); all suites green. | — |
| wrapper      | 9cf394c     | WS1: ClusterPack/PackExecution/PackInstance CRDs + deepcopy + 3 CRD YAMLs; WS2: ClusterPackReconciler (immutability enforcement, signature transition, SignaturePending requeue), PackExecutionReconciler (4-gate check, Kueue Job submission, OperationResult read); WS3: PackInstanceReconciler (PackReceipt drift/security, DependencyBlock); spec.lineage (SealedCausalChain) embedded in all 3 root-declaration specs, CRD YAMLs regenerated; 17 unit tests green; session/36 WS2 (bbb3361): ConductorReady gate 0 added to PackExecutionReconciler — isConductorReadyForCluster reads TalosCluster via unstructured from seam-tenant-{clusterRef}, checks ConductorReady condition; ConditionTypePackExecutionWaiting + ReasonAwaitingConductorReady added to types; 3 new gate 0 tests; all 4 existing gate tests updated with ConductorReady=True fixture; 20 unit tests green; Gap 27 WS2 resolved; session/39 (bacd59d): Gap 31 — wrapper/api/v1alpha1/lineage_conditions.go replaced with re-export pattern from seam-core/pkg/conditions; SC-INV-002 closed for wrapper. session/40 WS2 (de2a9a1): e2e suite — Ginkgo suite bootstrap, env vars, skip-all when MGMT_KUBECONFIG absent, make e2e target. session/40 WS4 (9cf394c): 2 e2e stub files (packexecution_test.go, packinstance_drift_test.go — 10 It blocks all skipping; each with pre-condition comment block); all suites green. | — |
| seam-core    | bbcc042     | LineageController complete (be0aaa1); session/24: WS2+WS3 — ILI immutability webhook (spec.rootBinding UPDATE rejected), controller-authorship gate (CREATE/UPDATE allowed only from lineage-controller SA), both wired into cmd/seam-core/main.go; 29 new unit tests green; session/37 WS1 (7f24eed): pkg/conditions — complete platform-wide condition vocabulary package; all 5 operator condition types and reason string constants; ValidateCondition/KnownConditionTypes/ValidReasonsFor helpers; 7 tests in 3 test functions (TestValidateCondition_AcceptsKnownPairs ~80 pairs, rejects unknown type/reason/empty, error message lists valid reasons, KnownConditionTypes sorted, ValidReasonsFor, alias drift check); 3 test suites green; session/37 WS2 (fcec232): api/v1alpha1/infrastructurelineageindex_types.go — local ConditionTypeLineageSynced/ReasonLineageControllerAbsent replaced with re-exports from pkg/conditions; SC-INV-002 closed for seam-core. session/40 WS1 (2370dbe): pkg/e2e — shared e2e helpers: ClusterClient (kubeconfig loader), ConditionPoller (polls status.conditions, validates against KnownConditionTypes, timeout with last-observed dump), CRApplier (SSA via dynamic client, FieldManager=seam-e2e), NamespaceEnsurer (seam.ontai.dev/webhook-mode label tiers: exempt/observe/enforce), RegistryClient (pure net/http OCI Distribution Spec); compiles clean, no test build tags. session/40 WS2 (5285c9e): e2e suite — Ginkgo suite bootstrap, env vars, skip-all when MGMT_KUBECONFIG absent, make e2e target. session/40 WS4 (bbcc042): 2 e2e stub files (lineage_controller_test.go, lineage_synced_test.go — 9 It blocks all skipping; each with pre-condition comment block); all 3 unit suites + e2e suite green. | — |
| domain-core  | f01ca65     | session/39: Layer 0 scaffold — api/v1alpha1: DomainLineageIndex (rootBinding immutable/descendantRegistry monotonic/policyBindingStatus updatable), 8 abstract business primitive stubs (DomainIdentity/Resource/Transaction/Policy/Event/Relationship/Ownership/Compliance), groupversion_info.go (core.ontai.dev), zz_generated.deepcopy.go; pkg/lineage: RootBindingRef, DescendantRef, IndexName helper; README: Layer 0 contract, instantiation rules (domain-core-schema.md §3), community extension pattern. No controllers, no domain-specific constraints. go build + go vet clean. domain-core-schema.md §2+§3+§4 satisfied. | — |

---

## 2. Open Findings

| ID    | Description                                                                    | Blocking?                              |
|-------|--------------------------------------------------------------------------------|----------------------------------------|
| F-S1  | Repo subdirectories not yet created in component repos                         | No                                     |
| F-S3  | ~~CRD YAML and DeepCopy are handwritten; controller-gen not wired~~ CLOSED Session 9 | Closed |
| F-S3B | KUBEBUILDER_ASSETS must be set manually for envtest runs                       | No (infra note)                        |
| F-S3C | ~~PermissionRule.Verbs requires typed Verb string~~ CLOSED Session 10 — Verb type added, CRD enum restored | Closed |
| F-6D  | ~~CapabilityRBACProvision executor-mode confirmed; implementation pending~~ CLOSED session/25 — conductor 2e2afa9 | Closed |
| F-P1  | ~~Day-2 CRD types and reconcilers absent in platform~~ CLOSED session/governor — platform f7f797b. All 8 day-2 types (EtcdMaintenance, NodeMaintenance, PKIRotation, ClusterReset, HardeningProfile, UpgradePolicy, NodeOperation, ClusterMaintenance) defined with spec.lineage; all 8 reconcilers implemented; wired into main.go; 12 unit tests green. | Closed |
| F-P2  | ~~Compiler subcommand model corrected~~ CLOSED session/governor (WS2) — conductor 2efc758. Compiler is a CR compiler: reads human-authored spec files and emits Kubernetes CR YAML only. Correct subcommands: `bootstrap`, `launch`, `enable` (all produce TalosCluster CR YAML), `packbuild` (produces ClusterPack CR YAML), `domain` (reserved). SOPS, Helm, and Kustomize are execution-mode concerns owned by Conductor Execute Mode — not Compiler concerns. Stubs return honest not-implemented errors; actual blockers are CR type integration from platform and wrapper libraries. | Closed |
| F-P3  | ~~Non-CAPI cluster lifecycle scope unverified~~ CLOSED session/governor — spec.capi.enabled=false is exclusively for management cluster bootstrap (thin Job submitter path). Target clusters always use CAPI. Day-2 node lifecycle (join, expansion, decommission) is handled by NodeOperation and UpgradePolicy CRDs delivered in F-P1. No gap found; no code changes required. | Closed |
| F-P5  | ~~MaintenanceBundle CRD definition absent~~ CLOSED session/29 WS2 — platform b00fcd5. CRD defined (platform.ontai.dev/v1alpha1), all spec fields (operation enum, maintenanceTargetNodes, operatorLeaderNode, s3ConfigSecretRef, spec.lineage), status conditions (ConditionTypeMaintenanceBundlePending initialized as stub, ConditionTypeLineageSynced), controller-gen CRD YAML generated, stub reconciler wired into main.go. Full RunnerConfig creation and Job submission remain deferred. compiler maintenance (conductor 2091ce0) produces MaintenanceBundle CR YAML with pre-resolved scheduling context. | Closed |
| F-P6  | ~~compiler component real implementation~~ CLOSED session/31 — conductor 6000606. WS1: internal/catalog/ embedded FS, 5 RBACProfile templates, 14 unit tests. WS2: compiler component subcommand (catalog/descriptor/discover modes), wired into main.go, 15 unit tests. 9 suites green. | Closed |
| F-P7  | ~~Update all existing day-2 reconcilers in platform to use the step list model~~ CLOSED session/33. WS1 (8db456d): all 6 job-submitting day-2 reconcilers migrated to RunnerConfig §17 multi-step model; OperationalRunnerConfig typed struct; 4-step NodeMaintenance; 2-step stack upgrade; S3 params in step.Parameters; ownerReference on all RCs; all tests green. WS2 (24ac26b): port-50000 retry/backoff; ControlPlaneUnreachable gate; PartialWorkerAvailability; 5 new tests green. | Closed |
| Gap-31 | ~~Condition vocabulary drift — each operator declares its own condition type/reason constants independently~~ FULLY RESOLVED session/39. pkg/conditions vocabulary package complete (seam-core 7f24eed); seam-core api/v1alpha1 migrated (fcec232); guardian migrated (8155ba2); platform migrated (871db38); wrapper migrated (bacd59d); conductor had no local condition constants — nothing to migrate. SC-INV-002 closed for all 5 components. | Closed |
| F-P8  | compiler enable phase 0 implementation — add CNPG operator manifests and CNPG Cluster CR to enable bundle output as 00-infrastructure-dependencies directory. Requires Conductor Engineer session. conductor-schema.md §9, guardian-schema.md §16. | No |

---

## 3. Role Reading Map

| Role                    | Required Documents (beyond CONTEXT.md)                                                       |
|-------------------------|----------------------------------------------------------------------------------------------|
| Governor                | PROGRESS.md, GIT_TRACKING.md, BACKLOG.md                                                     |
| Domain Architect        | *-schema.md for target domain                                                                |
| Schema Engineer         | Target *-schema.md + target component *-design.md + existing CRD YAML in that repo          |
| Controller Engineer     | guardian-schema.md + guardian/guardian-design.md + domain-core-schema.md + seam-core-schema.md (Guardian work) |
| Controller Engineer     | platform-schema.md + platform/platform-design.md + domain-core-schema.md + seam-core-schema.md (Platform work) |
| Controller Engineer     | wrapper-schema.md + wrapper/wrapper-design.md + domain-core-schema.md + seam-core-schema.md (Wrapper work)   |
| Conductor Engineer      | conductor-schema.md + conductor/conductor-design.md + all operator *-schema.md + domain-core-schema.md + seam-core-schema.md |
| Platform Engineer       | Target component *-schema.md + Dockerfile context for that component                        |
| Test Engineer           | Target *-schema.md + target component *-design.md                                           |
| Lab Operator            | ont-lab/ runbooks + CLAUDE.md §9                                                             |
| Release Engineer        | GIT_TRACKING.md, BACKLOG.md                                                                  |
| Incident Analyst        | PROGRESS.md + target component *-schema.md                                                   |

---

## 4. Namespace Model (Locked Governor Decision — 2026-04-05)

Three namespaces exist in the Seam platform. No operator may create or assume any other namespace without a Governor directive.

| Namespace | Purpose | Who owns it |
|-----------|---------|-------------|
| `seam-system` | Single operator namespace on the management cluster. All Seam operator managers (guardian, platform, wrapper, seam-core) run here. All leader election leases are held here. | All operators |
| `ont-system` | Conductor agent namespace. Exists on every cluster (management and target). Conductor agent Deployment runs here. Conductor execute-mode Jobs submitted by Wrapper run here on the management cluster. SnapshotStore, signing loop, and pull loop operate from this namespace. | Conductor |
| `seam-tenant-{cluster-name}` | One per managed cluster. ClusterPack, PackExecution, PackInstance, and all tenant-scoped objects live here. Created exclusively by the Platform operator (CP-INV-004). | Platform (creates); operators (read) |

Inventions such as `security-system`, `platform-system`, `infra-system`, `guardian-system`, or any unnamed variant are forbidden. They have been removed. This model is locked.

---

## 5. Next Session

**Role:** Lab Operator — lab provisioning
**Purpose:** Lab provisioning is the next gate. All e2e test scaffolding is complete (session/40). Tests are ready to run; they require live clusters.

**Pre-conditions met (session/40):**
- seam-core pkg/e2e shared helpers: ClusterClient, ConditionPoller, CRApplier, NamespaceEnsurer, RegistryClient (2370dbe)
- All 5 operator repos have Ginkgo e2e suite bootstrap + make e2e target
- All critical-path scenarios stubbed across all 5 repos (37 It blocks, all skipping)
- lab/topology.yaml created at ontai root: ccs-mgmt (3CP+2W) + ccs-test (3CP+2W), QEMU settings, node IPs, registry/port requirements, bootstrap sequences

**Pushes completed (session/40):**
- seam-core: bbcc042 on session/1-governor-init
- guardian: 34437c0 on session/1-governor-init
- platform: 2264170 on session/1-governor-init
- wrapper: 9cf394c on session/1-governor-init
- conductor: 4fbbffd on session/1-governor-init

**Immediate next implementation work (before e2e — e2e stubs for cross-cluster flows depend on this infrastructure):**
1. Guardian dual-role implementation session (heaviest session yet): GUARDIAN_ROLE env var gate, CNPG client, startup migration runner (guardian-schema.md §3), AuditSinkReconciler, AuditForwarderController, role-gated controller set registration. Assign Controller Engineer (guardian).
2. Conductor federation port and stream session: FEDERATION_PORT=9091 TLS setup with management CA client certs, bidirectional gRPC stream, typed message envelope (RunnerConfigValidationRequest/Response, AuditEventBatch, AuditEventAck, RevocationPush, HeartBeat), heartbeat discipline, degradation/reconnect behavior, WAL PVC buffer. Assign Conductor Engineer.
3. F-P8: compiler enable phase 0 — CNPG operator manifests + CNPG Cluster CR in 00-infrastructure-dependencies. Assign Conductor Engineer.

**Lab provisioning:** Blocked on QEMU lab instructions. Lab Operator role reads ont-lab/ runbooks + CLAUDE.md §9. Provision ccs-mgmt first (8-step bootstrap sequence per lab/topology.yaml), then ccs-test (5-step sequence). Once MGMT_KUBECONFIG and TENANT_KUBECONFIG are available, run `make e2e` in each repo to activate the test suite.

---
*Maintained by the Governor role. Refresh after every Governor session.*
*Section 4 (Namespace Model) locked 2026-04-05 by Governor directive.*
