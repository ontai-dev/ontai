# Seam Platform — Fast Bootstrap Context
> Read this file first. PROGRESS.md is the detailed audit record — consult on demand.

---

## 0. Anti-Patterns (FORBIDDEN)
- Adding Co-Authored-By trailers to commits
- Pushing the ontai root repository to a git remote — ontai root is intentionally not a git remote repository; do not attempt to push it
- Creating or assuming a namespace outside the canonical model (seam-system, ont-system, seam-tenant-{cluster-name}) without a Governor directive

## 1. Platform State

| Component    | Last Commit | Status                        | Next Pending Work                                           |
|--------------|-------------|-------------------------------|-------------------------------------------------------------|
| conductor    | 140e618     | WS1: SigningLoop signs PackInstance+PermissionSnapshot CRs with Ed25519 (SIGNING_PRIVATE_KEY_PATH gate, management cluster only, INV-026); WS2: local PermissionService gRPC (SnapshotStore + LocalService + hand-written service descriptor, PERMISSION_SERVICE_ADDR, all clusters); WS3: SnapshotPullLoop — target cluster pulls PermissionSnapshot from management, verifies Ed25519 (SIGNING_PUBLIC_KEY_PATH + MGMT_KUBECONFIG_PATH gates), calls SnapshotStore.Update; DegradedSecurityState on failure; bootstrap window mode (INV-020); session/25: CapabilityRBACProvision — Ed25519 signature verification (INV-026), bootstrap window bypass (INV-020), SigningPublicKey field added to ExecuteClients; session/governor (WS1-Compiler): Compiler subcommand model corrected — CR compiler (bootstrap/launch/enable/packbuild/domain) — F-P2 closed; session/governor (F-P4): WS1 three-image Dockerfile scaffolding (Dockerfile.compiler/execute/agent + docker-build Makefile target); WS2 RunnerConfig self-operation fields (MaintenanceTargetNodes/OperatorLeaderNode/SelfOperation + JobSpec.NodeExclusions + ResolveNodeExclusionsFromRunnerConfig + 5 new tests); WS3 compiler real implementations (platform+wrapper+seam-core local replace directives, compileBootstrap/compileLaunch/compileEnable produce TalosCluster CR YAML, compilePackBuild produces ClusterPack CR YAML); 7 suites green (F-P4 closed); session/29 WS3: compiler maintenance subcommand — resolves kubeconfig (flag→KUBECONFIG env→~/.kube/config), reads platform-leader Lease → pod → NodeName, validates S3 Secret for etcd-backup ops, validates target nodes, produces MaintenanceBundle CR YAML; 7 suites green; session/31 (F-P6): WS1: internal/catalog/ embedded FS with Go embed (entries/ YAML templates for Cilium/CNPG/Kueue/cert-manager/local-path-provisioner), CatalogEntry/RenderParams/ComponentDescriptor types, catalog.All()/Lookup()/AvailableNames()/Render()/DescriptorScaffold(); 14 unit tests in test/unit/catalog/; 8 suites green — 5eaceed; WS2: compiler component subcommand wired into main.go (catalog mode, descriptor mode, discover mode), compile_component.go + compile_component_test.go; 15 new tests; 9 suites green — 6000606. F-P6 closed. session/32: Compiler bootstrap/launch/enable real implementations. WS1 (d6bac86): compileBootstrap uses talos machinery (generate.NewInput, secrets.NewBundle, node-per-Secret, seam-mc-{cluster}-{hostname} naming, TalosCluster CR YAML, bootstrap-sequence.yaml, BootstrapSequence with json+yaml tags); 10 tests. WS2 (51264ca): compileLaunchBundle uses go embed from all 5 CRD repos (platform, guardian, wrapper, seam-core, conductor) — guardian added to go.mod replace block; crds.yaml bundle sorted+deterministic; 8 tests. WS3 (3745987): compileEnableBundle produces crds.yaml+operators.yaml+rbac.yaml+leaderelection.yaml+rbacprofiles.yaml; Conductor carries CONDUCTOR_ROLE=management (§15); all 5 operators with correct namespaces (conductor→ont-system, others→seam-system); RBACProfile CRs with review-required annotation; 11 tests. 9 suites green. session/governor (RunnerConfig Execution Model §17): WS1 (b62be77): RunnerConfigStep, StepPhase, ConfigMapRef, RunnerConfigStepResult added to runnerlib; Steps to RunnerConfigSpec, StepResults to RunnerConfigStatus; CRD YAML updated. WS2 (68d11f3): RunExecute replaced with step sequencer — StepExecutor + StepStatusWriter interfaces, NoopStepStatusWriter, capabilityStepExecutor in main.go; 9 kernel tests (single-step, multi-step, halt-on-failure, partial completion, error propagation). 9 suites green. session/35 (Gap 29, 38afdda): compileEnableBundle restructured to 5 phased subdirectories (01-guardian-bootstrap, 02-guardian-deploy, 03-platform-wrapper, 04-conductor, 05-post-bootstrap); each phase carries phase-meta.yaml (name, order, readinessGate, applyOrder); namespace-labels.yaml in phase 1 stamps seam.ontai.dev/webhook-mode=exempt on kube-system+seam-system as SSA metadata-only patches (satisfies guardian 25c9e93 WS3 CheckBootstrapLabels contract); CRDs filtered by API group from compileLaunchBundle; 15 enable tests all pass; 9 suites green. Gap 29 resolved. session/38 WS1 (d23a6fc): SigningLoop extended — after signing PackInstance CR, stores signed artifact as Secret seam-pack-signed-{clusterName}-{packInstanceName} in seam-tenant-{clusterName}; Secret data: artifact (base64 JSON) + signature (base64 Ed25519); idempotent (skip if sig matches, overwrite if differs); 3 new tests; 9 suites green. session/38 WS2 (f7a127e): PackInstancePullLoop — tenant cluster polls management cluster Secrets seam-pack-signed-{clusterName}-*, verifies Ed25519 signature (INV-026), creates/updates PackReceipt (Verified=True/False); bootstrap window mode (INV-020); idempotent; wired into agent.go alongside SnapshotPullLoop with shared mgmtDynamicClient; 9 unit tests; all 9 suites green. Gap 28 resolved. session/40 WS2 (19dcc03): e2e suite — Ginkgo suite bootstrap, MGMT_KUBECONFIG/TENANT_KUBECONFIG/REGISTRY_ADDR/MGMT_CLUSTER_NAME/TENANT_CLUSTER_NAME env vars, skip-all when MGMT_KUBECONFIG absent, make e2e target. session/40 WS4 (4fbbffd): 3 e2e stub files (signing_loop_test.go, packinstance_pull_loop_test.go, snapshot_pull_loop_test.go — 15 It blocks all skipping; each with pre-condition comment block); all suites green. session/42 WS1 (68823a9): federation channel TLS + connection lifecycle — tls.go (BuildServerTLSConfig with NextProtos:h2, BuildClientTLSConfig, ClusterIDFromCert), messages.go (Envelope + 6 MessageTypes + typed message structs), server.go (FederationServer + hand-written gRPC ServiceDesc, StartWithListener/Start, ClusterIDFromCert extraction, FEDERATION_PORT=9443 default), client.go (FederationClient, TLS dial, pendingAuditBatches chan, heartbeat discipline), SnapshotStore.Clear() added; wired into kernel/agent.go (server when all 3 FEDERATION_* certs set, client when MGMT_FEDERATION_ADDR set); 10 TLS unit tests green. session/42 WS2 (16cb289): bidirectional gRPC stream + typed messages — codec.go (federationJSONCodec registered in init(), ForceCodecOption()), server.go handleStream (TypeHeartBeat reset+ACK, TypeAuditEventBatch→ConfigMap in ont-system, TypeRunnerConfigValidationRequest stub approved), client.go receiveLoop (TypeHeartBeat ack-counter reset, TypeAuditEventAck→wal.Ack, TypeRevocationPush→SnapshotStore.Clear), heartbeatSender/heartbeatMonitor/auditBatchRelay goroutines; 8 stream unit tests green. session/42 WS3 (45bedf9): WAL PVC buffer — internal/federation/wal.go (append-only 4-byte-length-prefix wire format, Write/Ack/Replay/Compact/IsFull/SetOnFull, ErrWALFull, onFull callback synchronous after mutex release), client.go (SetWAL, write-before-send in SendAuditBatch, replayWAL at runOnce start, wal.Ack on AuditEventAck), kernel/agent.go (WAL_PATH/WAL_MAX_BYTES wiring, OpenWAL non-fatal); 9 WAL unit tests green. session/43 F-P8 (ee7adb0): compiler enable phase 0 — internal/catalog/cnpg/cnpg.go (go:embed OperatorManifest, Version=v1.24.0), internal/catalog/cnpg/operator.yaml (CNPG v1.24.0 static manifests: Namespace cnpg-system, 4 CRDs Cluster/Backup/ScheduledBackup/Pooler, SA/ClusterRole/CRB/Deployment), compile_enable.go (writePhase0InfrastructureDependencies + writeCNPGOperatorManifest + writeCNPGClusterCR: guardian-db in seam-system, instances=3, 50Gi, guardian-db-credentials, governance.infrastructure.ontai.dev/owner=seam-platform); 5 new phase 00 tests green. All 9 unit suites + compiler suite green. F-P8 closed. session/45 WS1 (07841cc): Compiler CLI help surface — printUsage rewritten; all 11 suites green. session/45 WS2 (06d1666): DSNS resources in 05-post-bootstrap; all 11 suites green. F-P9 closed. session/1-governor-init WS1 (2c4a014): FEDERATION_PORT default changed from 9091 to 9443 — tls.go DefaultFederationPort constant, all test references, conductor-schema.md §18; all 11 suites green. session/1-governor-init WS2 (08c0f55): CAPI manifests in compiler enable as optional 00b-capi-prerequisites phase — internal/catalog/capi/ (capi.go + 4 embedded YAMLs: capi-core v1.9.4, capi-talos-bootstrap v0.6.8, capi-talos-controlplane v0.5.8, seam-infrastructure-crds); --capi flag in runEnableSubcommand; writeCAPIPhaseMetaYAML (raw string, order: 0b); 8 new CAPI tests; all 11 suites green. session/1-governor-init WS3 (1e2b004): Prometheus metrics endpoints — standalone HTTP server in runAgent (METRICS_ADDR env, promhttp.HandlerFor(ctrlmetrics.Registry), goroutine); metrics Service manifests in enable phases 2/3/4 (guardian-metrics-service.yaml, platform-wrapper-metrics-services.yaml, conductor-metrics-service.yaml); prometheus/promhttp + controller-runtime/metrics deps added to go.mod; all 11 suites green. Pushed to origin/session/1-governor-init. Test Session E WS1 (8b33306): compile_maintenance.go refactored (compileMaintenanceCore + validateMaintenanceOperation extracted, three helpers widened to kubernetes.Interface); compile_packbuild_test.go (4 contract tests: produces YAML, APIVersion/Kind, spec fields, default namespace); compile_maintenance_test.go (6 tests using fake.NewSimpleClientset: drain produces bundle, etcd-backup embeds targets, clusterRef in output, leader node embedded, unknown operation validation, no-API-calls confirmation); compile_domain_test.go (subprocess pattern: exit code 1 + "reserved" in stderr). Test Session E WS2 (01869ef): TestBootstrap_MissingNameFieldYieldsDescriptiveError added; 4 PackBuild missing-field tests; validateComponentFlagConflict added to compile_component.go + wired into runComponentSubcommand; 3 mutual-exclusion tests; TestAnySubcommand_UnwritableOutputPathFails (ENOTDIR via file-as-parent). All 11 suites green. Test Session E complete. Test Session F WS3 (b3fdc93): envtest integration suite — test/integration/suite_test.go (envtest + dynamic client, k8sStepStatusWriter implements kernel.StepStatusWriter with real Kubernetes status patches, replaces NoopStepStatusWriter in test path); test/integration/runnerconfig_test.go (4 scenarios: management-role all-succeed→Completed in etcd, tenant-role succeed+no signing fields, two concurrent RunnerConfigs no cross-contamination, HaltOnFailure→Failed in etcd+post-step absent); go.mod/go.sum: json-patch/btree/fsnotify/go-logr/zapr transitive deps. All 11 unit suites green. Test Session F complete. session/1-governor-init WS1 (f9a411b): Makefile — add --platform linux/amd64, docker-push, lint-images targets for all three conductor images (compiler/conductor-execute/conductor); lint-images wired into lint. session/1-governor-init WS2 (140e618): compiler enable --registry flag — configurable OCI registry prefix (default 10.20.0.1:5000/ontai-dev); image name corrections (platform→ont-platform, wrapper→ont-infra); 2 new registry assertion tests; all 11 suites green. Pushed. | — |
| guardian     | 31d9ecf     | WS1: SealedCausalChain spec.lineage added to RBACPolicy, RBACProfile, IdentityBinding, IdentityProvider, PermissionSet; CRDs regenerated; seam-core dependency wired; WS2: LineageSynced=False/LineageControllerAbsent initialization in all 5 reconcilers; lineage_conditions.go added; WS3 (session/24): lineage_immutability.go + lineage_handler.go + RegisterLineage — rejects spec.lineage UPDATE on all 5 root-declaration kinds; 16 new unit tests green; session/34 WS1: namespace tiered admission mode (NamespaceModeResolver, KubeNamespaceModeResolver, StaticNamespaceModeResolver, seam.ontai.dev/webhook-mode label, exempt/observe/enforce tiers, ObservedDeny logging, 13 new unit tests); WS2: Guardian singleton CRD, WebhookModeGate, NamespaceEnforcementRegistry, GuardedNamespaceModeResolver, BootstrapController (Initialising→ObserveOnly ratchet, per-namespace enforcement promotion, 12 new unit tests); WS3: CheckBootstrapLabels startup gate — seam-system must carry webhook-mode=exempt before webhook registration, ErrBootstrapLabelAbsent, 5 new unit tests; 4 suites green; session/37 WS2 (8155ba2): lineage_conditions.go — local ConditionTypeLineageSynced/ReasonLineageControllerAbsent replaced with re-exports from seam-core/pkg/conditions; all 5 reconcilers compile unchanged via re-export pattern; SC-INV-002 closed for guardian. session/40 WS2 (ec9c701): e2e suite — Ginkgo suite bootstrap, env vars, skip-all when MGMT_KUBECONFIG absent, make e2e target. session/40 WS4 (34437c0): 3 e2e stub files (webhook_test.go, rbacprofile_test.go, identityprovider_test.go — 10 It blocks all skipping; each with pre-condition comment block); all suites green. session/41 WS1 (d851182): GUARDIAN_ROLE startup gate — internal/role: Role type, ParseRole, ReadFromEnv with overridable ExitFn, ControllerSetForRole (9 ControllerName constants); role-gated controller registration in main.go (setupSharedControllers + setupManagementControllers + setupTenantControllers); AuditSinkReconciler stub (WS3-ready) + AuditForwarderController stub (WS4-ready); 12 unit tests green. session/41 WS2 (49ebf9d): CNPG client and startup migration runner — internal/database/cnpg.go (ConnConfig, DB interface, 3 migrations: audit_events/permission_cache/identity_resolution_log, MigrationRunner, AuditDatabase interface, SQLAuditStore, AuditEvent); internal/database/secret.go (ConnConfigFromSecret from k8s Secret); internal/database/startup.go (RunWithRetry degraded-hold loop, 30s retry, CNPGUnreachable condition on Guardian singleton); 5 unit tests green using fake sql/driver — no real CNPG required. session/41 WS3 (325f967): full AuditSinkReconciler — deduplication on (cluster_id, sequence_number), InsertEvent, ConfigMap delete on success, requeue on DB error; 9 unit tests green. session/41 WS4 (6a9a506): full AuditForwarderController — flush on interval (30s default) + batch size threshold (100 default), JSON serialization into ConfigMap.Data["events"], staging in ont-system with audit-batch label + cluster-id annotation; 5 unit tests green. All unit suites green. session/1-governor-init WS3 (b1e76d3): METRICS_ADDR env var support — metricsDefault reads METRICS_ADDR at startup, falls back to :8080; ServiceMonitor deferred comment; all unit suites green. Test-Session-A WS1 (e482964): corrected CNPG migration runner unit tests — column-content verification for all 3 migrations (audit_events/permission_cache/identity_resolution_log), halt-on-failure test confirming N+1 not applied when N fails, RunWithRetry context-cancellation test, RunWithRetry CNPGUnreachable condition test (fake kube client + Guardian singleton); all 6 suites green. Test-Session-A WS2 (b21e383): RBACPolicy reconciler finalizer lifecycle (security.ontai.dev/rbacpolicy: add on first reconcile, remove on deletion per INV-006) + 4 reconciler unit tests: PermissionSetValid=True with existing PS, PermissionSetValid=False+PermissionSetNotFound+requeue with missing PS, finalizer blocks deletion until reconciler releases, re-validation fires when MaximumPermissionSetRef updated; all 6 suites green. Test-Session-A WS3 (37bb7b0): PermissionService corrected unit tests — active binding authorized response, no binding denied (not error), malformed/empty/null-byte principal no-panic structured response, ListPermissions all rules, WhoCanDo multiple principals, ExplainDecision full chain with MatchedRule+Reason+resource/apiGroup/verbs; all 6 suites green. Pushed to origin/session/1-governor-init. Test Session F WS1 (ba16ae2): envtest integration suite — test/integration/controller/ (scenarios 2/4/5/6 using controller_test TestMain with ClusterPackReconciler+IdentityProviderReconciler+RBACPolicyReconciler running against real API server); test/integration/lineage/ (scenario 7, separate TestMain, lineage immutability webhook — RegisterLineage only, separate testdata/lineage-validating-webhook-configuration.yaml, 3 tests: RBACPolicy mutation rejected, PermissionSet mutation rejected, CREATE with lineage allowed); config/webhook/lineage-validating-webhook-configuration.yaml (production webhook config). Bugs found: CRD only requires `type` for IdentityProvider, not `issuerURL` — reconciler sets Valid=False, not webhook; documented in test comments. Test Session F complete. Schema Engineer session WS1 (332a8a9): PermissionSnapshot CRD formal type definition — SubjectKind enum, PermissionEntry, SubjectEntry, SnapshotTimestamp, SigningKeyFingerprint, FreshnessWindowSeconds (default=300), Subjects []SubjectEntry, spec.lineage *SealedCausalChain, Signed bool, DriftDetected bool, Conditions []metav1.Condition; kubebuilder print columns; CRD YAML + DeepCopy regenerated. Schema Engineer session WS2 (0fd9cd1): PermissionSnapshotReconciler — LineageSynced init on first observation (one-time write), FreshnessCondition based on SnapshotTimestamp age vs FreshnessWindowSeconds, RequeueAfter FreshnessWindowSeconds; wired into setupSharedControllers (both roles); 6 unit tests green; all 7 guardian unit suites green. PermissionSnapshot schema gap closed. session/1-governor-init WS1 (31d9ecf): Dockerfile (distroless, cmd/ont-security→/usr/local/bin/guardian) + docker-build/docker-push/lint-images Makefile targets; lint-images wired into lint; IMAGE_REGISTRY=10.20.0.1:5000/ontai-dev IMAGE_NAME=guardian. | — |
| platform     | 19e7c4a     | TalosCluster + SeamInfrastructureCluster/Machine CRD types, all three reconcilers (TalosCluster, SIC, SIM), SIC/SIM CRDs with lineage + controller-gen clean, SIM implements 6-step machineconfig delivery via TalosMachineConfigApplier (only file with talos goclient, CP-INV-001 clean), unit tests green, go build clean; session/governor: all 8 day-2 CRD types (EtcdMaintenance, NodeMaintenance, PKIRotation, ClusterReset, HardeningProfile, UpgradePolicy, NodeOperation, ClusterMaintenance) with spec.lineage; all 8 day-2 reconcilers implemented with CapabilityUnavailable/PendingApproval gates; wired into main.go; 12 new unit tests green (F-P1 closed, F-P3 closed); session/29 WS1: resolveOperatorLeaderNode (platform-leader Lease → pod → NodeName) + jobSpecWithExclusions (NotIn NodeAffinity) wired into all 6 Job-submitting day-2 reconcilers; EtcdMaintenance S3 resolution hierarchy (EtcdBackupS3SecretRef spec field + seam-etcd-backup-config default + EtcdBackupDestinationAbsent condition); 6 new unit tests + 1 updated; all 17 tests green; session/29 WS2: MaintenanceBundle CRD (operation enum drain/upgrade/etcd-backup/machineconfig-rotation, maintenanceTargetNodes, operatorLeaderNode, s3ConfigSecretRef, spec.lineage); stub reconciler (F-P5); CRD YAML generated; wired into main.go; session/30 WS1: MaintenanceBundleReconciler full implementation — reads pre-encoded scheduling context, submits Job via jobSpecWithExclusions, transitions Pending→Ready/Degraded; JobName+OperationResult status fields; CRD YAML regenerated; 5 new tests; session/30 WS2: EnsureConductorDeploymentOnTargetCluster in TalosClusterReconciler — creates Conductor agent Deployment in ont-system on target cluster before marking Ready; CAPI kubeconfig Secret lookup; CONDUCTOR_ROLE=tenant env stamp; BuildConductorAgentDeployment; 3 new tests; all tests green; session/33 WS1 (8db456d): migrate day-2 reconcilers from batchv1.Job to RunnerConfig §17 multi-step model — OperationalRunnerConfig typed struct (runnerconfig_cr.go), 4-step NodeMaintenance RC (cordon→drain→operate→uncordon), 2-step UpgradeTypeStack RC (talos-upgrade→kube-upgrade DependsOn), S3 params in EtcdMaintenance backup step.Parameters, ownerReference on all RCs, all tests migrated to assert RunnerConfig list, new stack-upgrade and 4-step tests added, all tests green (F-P7 WS1 closed); session/33 WS2 (24ac26b): port-50000 retry/backoff — Port50000Backoff (10s base, 5min cap, 2^n formula), SeamInfrastructureMachineReconciler increments ApplyAttempts on failure and returns RequeueAfter=backoff with nil error, TalosClusterReconciler.checkMachineReachability lists SIMs after CAPI object creation — CP machine with ApplyAttempts>=3 sets ControlPlaneUnreachable=True and halts, worker sets PartialWorkerAvailability=True and continues, 5 new tests all green (F-P7 WS2 closed); session/36 WS1 (82533c8): ConductorReady condition — EnsureConductorDeploymentOnTargetCluster now returns (bool, error), checks Deployment Available condition after ensure; ensureConductorReadyAndTransition replaces both old call sites; ConditionTypeConductorReady/ReasonConductorDeploymentAvailable/Unavailable added to types; RemoteConductorAvailableFn injectable override for tests; 3 new unit tests (Available→Ready, Unavailable→requeue, condition transition); Gap 27 WS1 resolved; session/39 (871db38): Gap 31 — lineage_conditions.go in both platform/api/v1alpha1 and platform/api/infrastructure/v1alpha1 replaced with re-export pattern from seam-core/pkg/conditions; SC-INV-002 closed for platform. session/40 WS2 (60b0095): e2e suite — Ginkgo suite bootstrap, env vars, skip-all when MGMT_KUBECONFIG absent, make e2e target. session/40 WS4 (2264170): 3 e2e stub files (taloscluster_bootstrap_test.go, conductorready_test.go, port50000_test.go — 12 It blocks all skipping; each with pre-condition comment block); all suites green. session/1-governor-init WS3 (1a83871): METRICS_ADDR env var support — metricsDefault reads METRICS_ADDR at startup, falls back to :8080; ServiceMonitor deferred comment; all unit suites green. Test-Session-B (046bfec): WS1 — management bootstrap path tests (ManagementBootstrapJobSubmitted, ManagementBootstrapComplete), LineageSynced initialization + one-time write invariant; WS2 — RunnerConfig production tests (S3 tier 2 platform default fallback, EtcdMaintenance idempotency, clusterRef propagation); WS3 — Screen reserved path contract test (ScreenProviderNotImplemented=True when spec.infrastructureProvider=screen, field preserved, no operational conditions set; test will fail when Screen is implemented); API additions: TalosClusterSpec.InfrastructureProvider field, ConditionTypeScreenProviderNotImplemented + ReasonScreenNotImplemented constants, screen routing check in reconciler. All 10 new tests green, all existing tests remain green. Pushed to origin/session/1-governor-init. session/1-governor-init WS1 (19e7c4a): Dockerfile (distroless, cmd/ont-platform→/usr/local/bin/ont-platform) + docker-build/docker-push/lint-images Makefile targets; IMAGE_REGISTRY=10.20.0.1:5000/ontai-dev IMAGE_NAME=ont-platform. | — |
| wrapper      | 0308bbe     | WS1: ClusterPack/PackExecution/PackInstance CRDs + deepcopy + 3 CRD YAMLs; WS2: ClusterPackReconciler (immutability enforcement, signature transition, SignaturePending requeue), PackExecutionReconciler (4-gate check, Kueue Job submission, OperationResult read); WS3: PackInstanceReconciler (PackReceipt drift/security, DependencyBlock); spec.lineage (SealedCausalChain) embedded in all 3 root-declaration specs, CRD YAMLs regenerated; 17 unit tests green; session/36 WS2 (bbb3361): ConductorReady gate 0 added to PackExecutionReconciler — isConductorReadyForCluster reads TalosCluster via unstructured from seam-tenant-{clusterRef}, checks ConductorReady condition; ConditionTypePackExecutionWaiting + ReasonAwaitingConductorReady added to types; 3 new gate 0 tests; all 4 existing gate tests updated with ConductorReady=True fixture; 20 unit tests green; Gap 27 WS2 resolved; session/39 (bacd59d): Gap 31 — wrapper/api/v1alpha1/lineage_conditions.go replaced with re-export pattern from seam-core/pkg/conditions; SC-INV-002 closed for wrapper. session/40 WS2 (de2a9a1): e2e suite — Ginkgo suite bootstrap, env vars, skip-all when MGMT_KUBECONFIG absent, make e2e target. session/40 WS4 (9cf394c): 2 e2e stub files (packexecution_test.go, packinstance_drift_test.go — 10 It blocks all skipping; each with pre-condition comment block); all suites green. session/1-governor-init WS3 (84ecc98): METRICS_ADDR env var support — metricsDefault reads METRICS_ADDR at startup, falls back to :8080; ServiceMonitor deferred comment; all unit suites green. Pushed to origin/session/1-governor-init. Test Session C WS1 (348eb13): PackInstance creation added to PackExecutionReconciler success path (ownerRef to PackExecution, idempotent); test/unit/controller/ package — 15 new tests covering ownership chain (PE ownerRef to CP, PI ownerRef to PE), WaitingForCluster, INV-006 deletion path, Gates 0–4 with correct condition/reason assertions; 35 tests green. WS2 (3585abd): Kueue Job scheduling tests — queue label, pack artifact env vars, credentials volume mount, no DAG fields, Job failure→PE Failed/no-requeue, Job success→PE Succeeded+PI created, idempotency second reconcile; 42 tests green. Test Session C complete. Test Session F WS2 (6431789): envtest integration suite — test/integration/suite_test.go (ClusterPackReconciler+PackInstanceReconciler registered; PackExecution reconciler intentionally omitted — requires platform TalosCluster CRD not available in wrapper-only envtest; gate behavior covered by unit tests); test/integration/clusterpack_test.go (Scenario 1: ClusterPack accepted+SignaturePending=True; ObservedGeneration advances; Scenario 2: PackInstance ownerRef cascade — delete PackExecution, PI GC-deleted, validates real API server GC). Bugs found: SelfOperation is RunnerConfig field not PackExecution field; replaced with GC cascade test (higher value). Test Session F complete. session/1-governor-init WS1 (0308bbe): Dockerfile (distroless, cmd/ont-infra→/usr/local/bin/ont-infra) + docker-build/docker-push/lint-images Makefile targets; IMAGE_REGISTRY=10.20.0.1:5000/ontai-dev IMAGE_NAME=ont-infra. | — |
| seam-core    | 53a1d8d     | LineageController complete (be0aaa1); session/24: WS2+WS3 — ILI immutability webhook (spec.rootBinding UPDATE rejected), controller-authorship gate (CREATE/UPDATE allowed only from lineage-controller SA), both wired into cmd/seam-core/main.go; 29 new unit tests green; session/37 WS1 (7f24eed): pkg/conditions — complete platform-wide condition vocabulary package; all 5 operator condition types and reason string constants; ValidateCondition/KnownConditionTypes/ValidReasonsFor helpers; 7 tests in 3 test functions (TestValidateCondition_AcceptsKnownPairs ~80 pairs, rejects unknown type/reason/empty, error message lists valid reasons, KnownConditionTypes sorted, ValidReasonsFor, alias drift check); 3 test suites green; session/37 WS2 (fcec232): api/v1alpha1/infrastructurelineageindex_types.go — local ConditionTypeLineageSynced/ReasonLineageControllerAbsent replaced with re-exports from pkg/conditions; SC-INV-002 closed for seam-core. session/40 WS1 (2370dbe): pkg/e2e — shared e2e helpers: ClusterClient (kubeconfig loader), ConditionPoller (polls status.conditions, validates against KnownConditionTypes, timeout with last-observed dump), CRApplier (SSA via dynamic client, FieldManager=seam-e2e), NamespaceEnsurer (seam.ontai.dev/webhook-mode label tiers: exempt/observe/enforce), RegistryClient (pure net/http OCI Distribution Spec); compiles clean, no test build tags. session/40 WS2 (5285c9e): e2e suite — Ginkgo suite bootstrap, env vars, skip-all when MGMT_KUBECONFIG absent, make e2e target. session/40 WS4 (bbcc042): 2 e2e stub files (lineage_controller_test.go, lineage_synced_test.go — 9 It blocks all skipping; each with pre-condition comment block); all 3 unit suites + e2e suite green. session/44 WS1 (4f973b1): DSNS zone file management — internal/dns/zone.go (ZoneFile, AddRecord/RemoveRecord/Render, RFC 1035 SOA+NS+A+TXT+NS delegation, Unix timestamp serial, deterministic sort), internal/dns/configmap.go (ConfigMapZoneWriter, create with seam.ontai.dev/dsns-zone=true label + governance annotation, patch via MergeFrom), internal/dns/state.go (DSNSState shared zone+ownership map, UpdateRecords/RemoveRecords/SetStaticRecord/Apply, mutex-protected render before ConfigMap write); 8 unit tests. session/44 WS2 (fabd1d5): DSNSReconciler — internal/controller/dsns_reconciler.go (DSNSReconciler dispatches on GVK.Kind: TalosCluster→A+A+TXT or sovereign NS+glue A, IdentityBinding→identity TXT on TrustAnchorResolved=True, IdentityProvider→idp TXT on Valid=True, PackInstance→pack TXT on Ready=True, RunnerConfig→run TXT on Ready or Degraded terminal state; finalizer-gated deletion; best-effort IsNotFound cleanup; DSNSGVKs export); cmd/seam-core/main.go wired (DSNSState, CONDUCTOR_SIGNING_KEY_FINGERPRINT seeds authority.conductor static record, one DSNSReconciler per DSNSGVKs entry); 15 new unit tests. All 4 unit suites green. session/46 (8416641): NotificationSink extension point — internal/dns/event.go, sink_registry.go, state.go, dsns_reconciler.go; 17 new tests. All 4 suites green. Pushed. session/1-governor-init WS2 (35d3894): RetentionPolicy spec field added to InfrastructureLineageIndex — LineageRetentionPolicy struct (descendantRetentionDays int32 default=30 min=1, deleteWithRoot bool default=true); Group/Version/RecordedAt fields added to DescendantEntry; DeepCopy regenerated; CRD YAML regenerated; all 4 suites green. session/1-governor-init WS2 (b650579): retention enforcement in LineageController — pruneStaleDescendants (nil RecordedAt=keep; within window=keep; outside window + not-found=prune; API error=keep conservatively; patches ILI); ensureOwnerReferenceIfDeleteWithRoot (idempotent ownerRef addition, BlockOwnerDeletion=true, Kubernetes GC cascade); 5 new unit tests green. session/1-governor-init WS3 (b4fc511): METRICS_ADDR env var support — metricsDefault reads METRICS_ADDR at startup, falls back to :8080; ServiceMonitor deferred comment; all 4 unit suites green. Pushed to origin/session/1-governor-init. Schema Engineer session (0a0948a): ConditionTypePermissionSnapshotFresh="Fresh" + ReasonSnapshotFresh/ReasonSnapshotStale; ConditionTypePermissionSnapshotCompliant="Compliant" + ReasonDriftAbsent (new) + ReasonDriftDetected (reused); both added to ValidateCondition vocabulary in validate.go; all 4 seam-core unit suites green. Pushed. session/1-governor-init WS1 (53a1d8d): Dockerfile (distroless, cmd/seam-core→/usr/local/bin/seam-core) + docker-build/docker-push/lint-images Makefile targets; IMAGE_REGISTRY=10.20.0.1:5000/ontai-dev IMAGE_NAME=seam-core. | — |
| domain-core  | f01ca65     | session/39: Layer 0 scaffold — api/v1alpha1: DomainLineageIndex (rootBinding immutable/descendantRegistry monotonic/policyBindingStatus updatable), 8 abstract business primitive stubs (DomainIdentity/Resource/Transaction/Policy/Event/Relationship/Ownership/Compliance), groupversion_info.go (core.ontai.dev), zz_generated.deepcopy.go; pkg/lineage: RootBindingRef, DescendantRef, IndexName helper; README: Layer 0 contract, instantiation rules (domain-core-schema.md §3), community extension pattern. No controllers, no domain-specific constraints. go build + go vet clean. domain-core-schema.md §2+§3+§4 satisfied. | — |

---

## 2. Open Findings

| ID    | Description                                                                    | Blocking?                              |
|-------|--------------------------------------------------------------------------------|----------------------------------------|
| F-S1  | Repo subdirectories not yet created in component repos                         | No                                     |
| F-S3  | ~~CRD YAML and DeepCopy are handwritten; controller-gen not wired~~ CLOSED Session 9 | Closed |
| F-S3B | KUBEBUILDER_ASSETS must be set manually for envtest runs                       | No (infra note)                        |
| F-S3C | ~~PermissionRule.Verbs requires typed Verb string~~ CLOSED Session 10 — Verb type added, CRD enum restored | Closed |
| F-6D  | ~~CapabilityRBACProvision executor-mode confirmed; implementation pending~~ CLOSED session/25 — conductor 2e2afa9 | Closed |
| F-P1  | ~~Day-2 CRD types and reconcilers absent in platform~~ CLOSED session/governor — platform f7f797b. All 8 day-2 types (EtcdMaintenance, NodeMaintenance, PKIRotation, ClusterReset, HardeningProfile, UpgradePolicy, NodeOperation, ClusterMaintenance) defined with spec.lineage; all 8 reconcilers implemented; wired into main.go; 12 unit tests green. | Closed |
| F-P2  | ~~Compiler subcommand model corrected~~ CLOSED session/governor (WS2) — conductor 2efc758. Compiler is a CR compiler: reads human-authored spec files and emits Kubernetes CR YAML only. Correct subcommands: `bootstrap`, `launch`, `enable` (all produce TalosCluster CR YAML), `packbuild` (produces ClusterPack CR YAML), `domain` (reserved). SOPS, Helm, and Kustomize are execution-mode concerns owned by Conductor Execute Mode — not Compiler concerns. Stubs return honest not-implemented errors; actual blockers are CR type integration from platform and wrapper libraries. | Closed |
| F-P3  | ~~Non-CAPI cluster lifecycle scope unverified~~ CLOSED session/governor — spec.capi.enabled=false is exclusively for management cluster bootstrap (thin Job submitter path). Target clusters always use CAPI. Day-2 node lifecycle (join, expansion, decommission) is handled by NodeOperation and UpgradePolicy CRDs delivered in F-P1. No gap found; no code changes required. | Closed |
| F-P5  | ~~MaintenanceBundle CRD definition absent~~ CLOSED session/29 WS2 — platform b00fcd5. CRD defined (platform.ontai.dev/v1alpha1), all spec fields (operation enum, maintenanceTargetNodes, operatorLeaderNode, s3ConfigSecretRef, spec.lineage), status conditions (ConditionTypeMaintenanceBundlePending initialized as stub, ConditionTypeLineageSynced), controller-gen CRD YAML generated, stub reconciler wired into main.go. Full RunnerConfig creation and Job submission remain deferred. compiler maintenance (conductor 2091ce0) produces MaintenanceBundle CR YAML with pre-resolved scheduling context. | Closed |
| F-P6  | ~~compiler component real implementation~~ CLOSED session/31 — conductor 6000606. WS1: internal/catalog/ embedded FS, 5 RBACProfile templates, 14 unit tests. WS2: compiler component subcommand (catalog/descriptor/discover modes), wired into main.go, 15 unit tests. 9 suites green. | Closed |
| F-P7  | ~~Update all existing day-2 reconcilers in platform to use the step list model~~ CLOSED session/33. WS1 (8db456d): all 6 job-submitting day-2 reconcilers migrated to RunnerConfig §17 multi-step model; OperationalRunnerConfig typed struct; 4-step NodeMaintenance; 2-step stack upgrade; S3 params in step.Parameters; ownerReference on all RCs; all tests green. WS2 (24ac26b): port-50000 retry/backoff; ControlPlaneUnreachable gate; PartialWorkerAvailability; 5 new tests green. | Closed |
| Gap-31 | ~~Condition vocabulary drift — each operator declares its own condition type/reason constants independently~~ FULLY RESOLVED session/39. pkg/conditions vocabulary package complete (seam-core 7f24eed); seam-core api/v1alpha1 migrated (fcec232); guardian migrated (8155ba2); platform migrated (871db38); wrapper migrated (bacd59d); conductor had no local condition constants — nothing to migrate. SC-INV-002 closed for all 5 components. | Closed |
| F-P8  | ~~compiler enable phase 0 implementation~~ CLOSED session/43 — conductor ee7adb0. 00-infrastructure-dependencies phase added as first phase in compileEnableBundle: CNPG v1.24.0 operator manifests embedded at compile time (internal/catalog/cnpg/), guardian-db Cluster CR in seam-system (3 instances, 50Gi, guardian-db-credentials secretName); 5 new tests green. | Closed |
| F-P9  | ~~DSNS controller implementation in seam-core~~ CLOSED session/44+45. DSNSReconciler complete (seam-core fabd1d5): ZoneFile, ConfigMapZoneWriter, DSNSState, DSNSReconciler for all 5 GVKs, main.go wired. DSNS phase 5 resources complete (conductor 06d1666): dsns-zone-configmap.yaml, coredns-dsns-stanza.yaml, dsns-loadbalancer.yaml. All 11 conductor suites green. | Closed |
| BL-1  | ServiceMonitor CRDs for Prometheus Operator scrape configuration — all five operators expose :8080/metrics but no ServiceMonitor CRs exist; deferred to post-e2e observability session. Add ServiceMonitor YAMLs + enable phase for Prometheus Operator CRDs. | No |
| BL-2  | CAPI-path cluster e2e tests — the 00b-capi-prerequisites enable phase is implemented and tested in unit scope only; no e2e test validates CAPI provider bring-up or SeamInfrastructureCluster/Machine lifecycle against a live cluster. | No |
| BL-3  | Lineage retention tuning under load — pruneStaleDescendants patches ILI on every reconcile when entries expire; under high descendant churn this may cause excessive API write traffic; measure and gate prune calls behind minimum reconcile interval or batch threshold. | No |

---

## 3. Role Reading Map

| Role                    | Required Documents (beyond CONTEXT.md)                                                       |
|-------------------------|----------------------------------------------------------------------------------------------|
| Governor                | PROGRESS.md, GIT_TRACKING.md, BACKLOG.md                                                     |
| Domain Architect        | *-schema.md for target domain                                                                |
| Schema Engineer         | Target *-schema.md + target component *-design.md + existing CRD YAML in that repo          |
| Controller Engineer     | guardian-schema.md + guardian/guardian-design.md + domain-core-schema.md + seam-core-schema.md (Guardian work) |
| Controller Engineer     | platform-schema.md + platform/platform-design.md + domain-core-schema.md + seam-core-schema.md (Platform work) |
| Controller Engineer     | wrapper-schema.md + wrapper/wrapper-design.md + domain-core-schema.md + seam-core-schema.md (Wrapper work)   |
| Conductor Engineer      | conductor-schema.md + conductor/conductor-design.md + all operator *-schema.md + domain-core-schema.md + seam-core-schema.md |
| Platform Engineer       | Target component *-schema.md + Dockerfile context for that component                        |
| Test Engineer           | Target *-schema.md + target component *-design.md                                           |
| Lab Operator            | ont-lab/ runbooks + CLAUDE.md §9                                                             |
| Release Engineer        | GIT_TRACKING.md, BACKLOG.md                                                                  |
| Incident Analyst        | PROGRESS.md + target component *-schema.md                                                   |

---

## 4. Namespace Model (Locked Governor Decision — 2026-04-05)

Three namespaces exist in the Seam platform. No operator may create or assume any other namespace without a Governor directive.

| Namespace | Purpose | Who owns it |
|-----------|---------|-------------|
| `seam-system` | Single operator namespace on the management cluster. All Seam operator managers (guardian, platform, wrapper, seam-core) run here. All leader election leases are held here. | All operators |
| `ont-system` | Conductor agent namespace. Exists on every cluster (management and target). Conductor agent Deployment runs here. Conductor execute-mode Jobs submitted by Wrapper run here on the management cluster. SnapshotStore, signing loop, and pull loop operate from this namespace. | Conductor |
| `seam-tenant-{cluster-name}` | One per managed cluster. ClusterPack, PackExecution, PackInstance, and all tenant-scoped objects live here. Created exclusively by the Platform operator (CP-INV-004). | Platform (creates); operators (read) |

Inventions such as `security-system`, `platform-system`, `infra-system`, `guardian-system`, or any unnamed variant are forbidden. They have been removed. This model is locked.

---

## 5. Next Session

**Role:** Lab Operator
**Purpose:** Lab bring-up (sessions G through J). All implementation work is complete across all five repos. Run `make cluster-up` for ccs-mgmt and verify cluster readiness.

**Pre-conditions met (Schema Engineer + pre-lab compatibility test + targeted fix sessions complete):**
- conductor: 140e618 (session/1-governor-init) — compiler enable --registry flag (default 10.20.0.1:5000/ontai-dev), image names corrected (ont-platform/ont-infra), all 11 suites green; Makefile --platform linux/amd64 + docker-push + lint-images for all 3 conductor images; pushed. Previous: WS1 (316b540): importExistingCluster PKI extraction replaced: secrets.LoadBundle removed; Compiler connects via kubeconfig (--kubeconfig flag → KUBECONFIG → ~/.kube/config), reads seam-mc-{cluster}-{init-node} Secret from seam-system, parses machineconfig.yaml with configloader.NewFromBytes, derives secrets.Bundle via secrets.NewBundleFromConfig + Validate(); --talosconfig removed, --kubeconfig added. WS2 (b6e3dee): CNPG credentials generation — writeGuardianDBCredentials uses crypto/rand 32-byte password (base64url), emits guardian-db-credentials.yaml (Opaque, username=postgres) in phase 00 BEFORE cnpg-cluster.yaml; Guardian Deployment gains CNPG_SECRET_NAME=guardian-db-app, CNPG_SECRET_NAMESPACE=seam-system, GUARDIAN_ROLE=management; 3 new tests. WS3: compiler (81M) + conductor (85M) binaries built clean. All 11 suites green. Previously: a366a63 — ClusterInput patches/registryMirrors/ciliumPrerequisites/importExistingCluster; phase 00a-namespaces; all pushed.
- lab/CLAUDE.md: exact Cilium Helm values recorded (all flags marked required for Talos); kernel module + sysctl requirements documented in compile.go comment, topology.yaml, and lab/CLAUDE.md
- lab/configs/ccs-mgmt/cluster-declaration.yaml: ciliumPrerequisites=true, four registryMirrors entries, commented documentation for all new fields
- guardian: 31d9ecf — Dockerfile + docker-build/push/lint-images Makefile targets added; IMAGE_NAME=guardian; pushed
- platform: 19e7c4a — Dockerfile + docker-build/push/lint-images Makefile targets added; IMAGE_NAME=ont-platform; pushed
- wrapper: 0308bbe — Dockerfile + docker-build/push/lint-images Makefile targets added; IMAGE_NAME=ont-infra; pushed
- seam-core: 53a1d8d — Dockerfile + docker-build/push/lint-images Makefile targets added; IMAGE_NAME=seam-core; pushed
- ontai root Makefile: build-all (seam-core→guardian/platform/wrapper parallel→conductor) + push-all targets; IMAGE_REGISTRY/TAG overridable; lint-docs + install-hooks wired
- lab/scripts/ (01–05) + lab/Makefile present in ontai root (committed, not pushed to any remote)
- All F-P* findings closed; three backlog items (BL-1/2/3) logged for post-e2e

**Lab Operator session scope:**
- Read ont-lab/ runbooks before beginning
- Use lab/Makefile targets: golden → cluster-up → verify
- Do not push ontai root to any remote

---
*Maintained by the Governor role. Refresh after every Governor session.*
*Section 4 (Namespace Model) locked 2026-04-05 by Governor directive.*
