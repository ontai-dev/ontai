# Constitutional documents — not committed to public git
CLAUDE.md
AGENTS.md

# Nested component repositories — each has its own git history
conductor/
domain-core/
guardian/
platform/
wrapper/
seam-core/

# Lab infrastructure files — not committed
lab/

# Claude project context — never committed
.claude/

# Architecture working documents — generated files, not version-controlled here
ONT-Seam-Architecture.pptx
build_pptx.py

# Credentials and keys
*.pem

# OS
.DS_Store
Thumbs.db

# Markdown: tracked files
*.md
!README.md
!*-schema.md
!CONTEXT.md
!BACKLOG.md
!PROGRESS.md
!GIT_TRACKING.md
!CRD_OWNERSHIP_AUDIT.md
!docs/**

# Graphify knowledge graph — source of truth for all agents
!graphify-out/
!graphify-out/**
guardian-db-credentials.yaml
